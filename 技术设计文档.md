# 模型评测平台 · 技术设计文档（修订版）

## 1. 概述

### 1.1 范围
本文档基于《需求迭代文档》，描述从「双模型对决」演进为「N 模型横向 benchmark」评测平台的技术方案。覆盖：目标架构、数据模型、核心流程、模块设计、API、前端、安全、存储兼容、测试策略。

### 1.2 读者
后端/前端开发、测试、部署维护人员。

### 1.3 术语

| 术语 | 含义 |
|---|---|
| job | 一次评测执行单元（任务集 × 单模型作答流） |
| batch | benchmark 批次：1 任务集 × N 模型 × M 轮重复 |
| verdict | 评审结果（逐题 X/Y 分或单臂分） |
| 单臂评审 | 每题对单个模型按 rubric 独立评分，无需配对 |
| 两两对决 | 双盲 X/Y 配对比较（现形态） |
| 金标集 | 人工权威评分题（用于评审元评估与单臂锚定） |
| gen_config | LLM 出题所使用模型的独立配置 |

### 1.4 设计原则
1. **零新依赖优先**：BLEU/ROUGE、bootstrap CI 纯 Python 实现；embedding 外部 provider 优先，本地 BGE 懒加载
2. **向后兼容**：现有数据集/历史记录格式零破坏；新字段可缺省；review_mode 默认 pure_human 保持现有人工流程
3. **安全不变量**：Key 不落盘、SSRF 校验、审计、限流对新接口自动生效
4. **扩展点先行**：metrics 注册表、任务类型注册表、评审方协议、调度器契约在迭代 0 定义
5. **复杂度控制**：v1 不做队列持久化、不做评测包导入，一切以"不引入新基础设施"为准

## 2. 现状架构

### 2.1 模块结构

```
backend/
├── main.py            # FastAPI 路由 + 内存 _jobs/_tasks + 后台协程调度（38KB 单文件）
├── schemas.py         # Pydantic 模型（StartRequest 仅 model_a/model_b）
├── storage.py         # .eval/history/<jobId>/*.json；data/datasets/*.json
├── access.py          # Host/Origin 校验、限流、SSE ticket 清理
├── audit.py / security.py / sse_ticket.py / ssrf.py
└── engine/
    ├── tasks.py       # 内置题库（七维）+ 任务集生成
    ├── executor.py    # 每模型串行逐题调用，双模型 gather 并发；单题重试1次
    ├── judge.py       # 双盲 AI 评审（已实现但未接入主流程）
    ├── human_review.py# 人工双盲评审（当前唯一生效路径）
    ├── report_builder.py / parsers.py / datasets.py / mock.py
    └── isolation/     # 代码沙箱（off / Windows 原生）
frontend/  index.html（配置页） report.html（报告） review.html（评审） echarts.min.js
```

### 2.2 现状关键约束
- **状态机由文件推断**：`pending → executing → reviewing → completed | error`（storage._job_state）
- **并发上限硬编码**：`MAX_ACTIVE_JOBS = 2`（main.py:358），超限 429 拒绝，无排队
- **评审闭环**：作答完成后进入 reviewing，由前端人工打分提交，`judge.py` 未调用
- **双盲映射**：`human_review.make_reveal` 每轮一次全局随机，位置偏差只消除一半
- **Key 生命周期**：仅内存（持久化 `key_masked: "***"`），`redact_sensitive` 递归脱敏
- **逐题归一化基础**：`human_review.resolve_round` 已按题解析 answer_x/answer_y（reveal 可降粒度到题，低成本）

## 3. 目标架构

### 3.1 新增模块

```
backend/
├── models_registry.py   # 模型配置库（N 套，Key 不落盘）
├── scheduler.py         # 队列/优先级/并发配额/批处理（替代硬编码并发2，v1 内存队列）
├── engine/
│   ├── metrics.py       # 指标注册表 + 计算（判别式/生成式分支）
│   ├── embed.py         # embedding provider 抽象（external / local-bge 懒加载）
│   ├── reviewer.py      # 评审方协议（AgentReviewer / HumanReviewer / 混合）
│   ├── extract.py       # 答案提取分层（规则 → LLM 解析）
│   ├── generator.py     # LLM 出题 pipeline（生成/校验/安全过滤/入库）
│   ├── perturb.py       # 对抗/偏见扰动管线
│   ├── badcase.py       # 错误分类/自动归因/挖掘
│   └── stats.py         # 显著性（胜率/bootstrap CI/相关性/Kappa）
├── judge.py（改造）      # 逐题独立随机交换 + 单臂模式 + 健康度
└── main.py（重构）       # 路由薄化，调度逻辑下沉 scheduler
```

### 3.2 模块依赖图

```
前端(配置库/评测/审核/任务列表/报告/排行榜)
  │ REST + SSE(ticket)
main.py ── 路由薄层
  ├── scheduler.py ── 队列/优先级/配额 ── benchmark batch
  │     └── 执行单元: executor.py（单模型流）
  ├── models_registry.py / storage.py
  └── 评审链路: reviewer.py ── AgentReviewer→judge.py / HumanReviewer→human_review.py
          └── metrics.py ── embed.py / extract.py / stats.py
generator.py ── gen_config 出题 → 校验 → storage(评测集)
badcase.py  ── report 数据 → 归因 → 反馈
perturb.py  ── 扰动 → executor 评测 → 衰减曲线
安全横切: access/audit/ssrf/sse_ticket 对所有新接口自动生效
```

## 4. 数据模型设计

### 4.1 task schema（迭代 1 落地）

```json
{
  "id": "T1",
  "type": "判别式|生成式",
  "dimension": "八维之一",
  "difficulty": "easy|medium|hard",
  "tags": [],
  "prompt": "...",
  "expected": "判别式必填；生成式可选",
  "rubric_note": "生成式必填",
  "context": "可选（RAG/grounding 评测载体）",
  "test_cases": []
}
```

校验规则（datasets.py 扩展，迭代 1 实测落地）：
- 显式声明 `type=判别式`：`expected` 或 `test_cases` 至少其一（test_cases 每项含 `input` 与 `expected`）
- `type=生成式`：`expected` 可空但 `rubric_note` 非空（含自动打分协议）；缺省 rubric 由 `BUILTIN_RUBRICS` 兜底
- `type` 缺省 = 判别式；未显式 type 的旧格式任务宽容放行，构建阶段补缺省 rubric，零破坏
- `context` 长度上限 32000 字符（防注入过载）；`tags` 每项 ≤50 字符、每题 ≤10 个
- **实现坑**：`explicit_type` 必须在 `t["type"]` 缺省赋值前捕获，否则恒为 True

### 4.2 模型配置库

```
webapp/data/models/<id>.json
{ "id": "m1", "name": "gpt-4o", "url": "...", "temperature": 0.7,
  "max_tokens": 4096, "top_p": null, "key_masked": "***" }
```

- **Key 不落盘**：注册接口仅接收 URL/名称/参数；Key 存进程内存表 `{id: key}`，重启后需补录（页面提示）
- 列表经脱敏（`key_masked` + `has_key` 布尔）；**单条 `GET /api/models/{id}` 附带进程内存 Key**（供前端「填入」流程，Key 未补录时返回空并提示手动填写）；删除同步清内存 Key
- 重名拒绝（400）；删除不存在的 id 返回 404
- SSRF：注册即校验 URL（`validate_upstream_url`，测试环境 `ALLOW_PRIVATE` 未开启，合法 URL 须公网 IP）
- **出题/评审/embedding 等辅助模型配置**：独立字段（`gen_config`/`judge_config`/`embedding_config`）与模型库解耦，由页面或 API 显式传入，不上模型库

### 4.3 job 状态机（扩展）

```
queued ─→ executing ─→ judging(AI 评审) ─→ completed
   │          │   └────── reviewing(人工) ──┘   （混合模式：人工复核路径）
   │          └──→ error / cancelled
   └──→ cancelled（排队中可取消）
```

- `storage._job_state` 补充：`queued`（无 tasks.json）、`judging`（有 verdict.json 且无 report.json）
- 新增 `manifest.json`：job 元数据（batch_id、模型槽位、环境快照指纹）
- **排队期语义**：job 提交（submit）即创建 `sse_queue`，排队中即可连接 SSE 收 `queued` 事件；排队中取消走独立分支（从队列移除 + 置 `cancelled` + 审计 `eval_cancelled`），与运行中取消（`cancelling` → task.cancel）区分

### 4.4 batch（benchmark 批次）

```
.eval/batches/<batch_id>.json
{ "batch_id": "...", "dataset_snapshot": "v3@hash", "models": ["m1","m2","m3"],
  "rounds": 1, "jobs": ["jid1","jid2","jid3"], "state": "running|done|partial",
  "started_at": ..., "finished_at": ... }
```

每模型一个 job（执行单元），共享同一任务集快照；批次完成聚合出排行榜。

### 4.5 跨 job 历史汇总（迭代 0/5）

- `.eval/stats/saturation.json`（按 `数据集版本+题id` 聚合历史得分率）
- 写入时机：job 完成时增量更新（按 job_id 幂等去重）
- **不计分题语义（迭代 1 落地）**：聚合仅统计 `scoring_ids`（排除 `excluded_from_total` 题），meta 记录 `excluded_ids`/`excluded_dimensions`；报告 summary 亦以 scoring_ids 为准（win/ties/avg/max_score/scoring_count），不计分维度单独展示
- 接口：`GET /api/stats/saturation?dataset=...`（附 `stats_dir`/`saturation_file` 供测试隔离）

### 4.6 bad case 记录

```
.eval/badcases/<case_id>.json
{ "case_id", "job_id", "task_id", "category": "事实|语义|推理|安全偏见|执行",
  "model", "score", "evidence": {"verdict": ...},
  "attribution": {"label": "...", "by": "llm|human", "confirmed": false} }
```

### 4.7 环境快照（评测包导出）

```
.eval/history/<job_id>/env.json
{ "seed", "temperature", "model_names", "judge_config_fingerprint",
  "dataset_version", "dims", "repeat_n", "platform_version" }
```

评测包 = `zip(jobs/*, batches/*, env.json, tasks.json)` + hash 清单。**仅提供导出**（第三方复核），不提供导入。

## 5. 核心流程设计

### 5.1 评测主流程（AI 评审接入后）

```
start_eval → 建 job(queued, 即建 sse_queue) → scheduler 派发(executing)
  → executor：逐题作答，progress_cb 挂增量落盘钩子（断点续跑依据）
  → 评审方调度：
      pure_agent → judge.py（逐题随机交换+单臂/对决）→ verdict.json → completed
      hybrid → judging 后抽 Top 分歧案例 → reviewing（人工复核）→ 提交 → completed
      pure_human → 维持现状（等待前端提交）
  → report_builder 生成报告（指标/元评估/环境快照）
```

- 取消语义：排队中 → 独立移除分支；运行中 → 沿用 `cancelling` → task.cancel；终态删除语义不变
- 断点续跑：answers 增量落盘后，恢复时按 `attempts/status` 跳过已成功题

### 5.2 评审流程

**逐题独立随机交换**：每题 `make_reveal` 独立生成映射，`round-verdicts.json` 逐题记录 reveal；聚合按 reveal 归一化（复用 `human_review.resolve_round` 逐题能力）。

**评审方协议（reviewer.py）**：

```python
class Reviewer(Protocol):
    async def review_task(task, answers) -> Verdict
    async def review_all(task_set, answers_map) -> Verdicts
```

- `AgentReviewer`：包装 judge.py；`HumanReviewer`：包装 human_review.py（提交语义不变）
- 策略配置：`pure_agent / pure_human / hybrid`（hybrid = Agent 预评 + 分歧/低分 Top-K 转人工复核，K 可配置）

**单臂模式（N 模型）**：reviewer 对每模型独立调用（rubric 评分）。**金标锚定 v1 实现**：线性偏移 `score' = score + (gold_score - raw_gold_avg)`（用金标题上人工分与评审分的均值差校准评审偏置），复杂校准（分段/回归）后置。

**健康度与降级**：invalid 率 > 10% 告警；评审连续失败 N 次 → 自动降级人工（fail-open/fail-closed 可配置）。

### 5.3 benchmark 批次流程

```
POST /api/benchmark { dataset, model_ids[], rounds, review_mode, ... }
  → 校验模型配置/SSRF → 生成 batch_id + N 个 job 入队
  → scheduler 按并发配额派发（并行上限 = 配额）
  → 全部 job 终态 → 聚合 metrics → 排行榜落盘
  → 预算熔断：预估 token = 题数×rounds×N×评审倍率，超限拒绝/提醒
```

**部分失败聚合规则**：任一 job `error` 时 batch 置 `partial`；排行榜**排除失败模型并标注 N/A**，其余模型正常排名；失败原因随报告展示。

### 5.4 LLM 出题 pipeline

```
模板（类型×维度）→ gen_config 模型生成 → 自动校验:
  ① 去重（与现有题库相似度阈值）
  ② 可解性（生成参考答案自检 + 判别式验证）
  ③ rubric 完整性（生成式必填，缺失重生成）
  ④ 防泄漏（与已知公开题集合相似度低）
  ⑤ 内容安全过滤（有害词表 + LLM 审核双保险）
→ 待审核入库（pending_review）→ 人工审核页 → approved → 版本化快照
```

**出题模型来源**：`gen_config` 独立配置（默认复用 `judge_config`，均可页面/API 显式指定；与被测模型无关）。

### 5.5 扰动管线（对抗/偏见）

```
perturb(task, mode): mode ∈ {改写, 噪声注入, 属性扰动(性别/地域/文化), 污染}
  → 扰动版任务集（id 加后缀 -pN）→ 同一模型跑原版+扰动版
  → score_original vs score_perturbed → 衰减曲线 / 偏见一致性
```

复用 generator 校验能力，扰动题同样过内容安全过滤。

## 6. 关键模块设计

### 6.1 metrics 注册表

```python
METRICS: dict[str, MetricImpl] = {
  "top1": ..., "f1": ..., "exact_match": ..., "relaxed_accuracy": ...,  # 判别式
  "semantic_sim": ..., "rubric_score": ..., "consistency": ...,
  "bleu": ..., "rouge": ...,                                            # 生成式
}
def compute(task_type, task, answer, expected, ctx) -> MetricResult
```

纯函数实现（`(task, answer, expected) → float`），按 task.type 路由；BLEU/ROUGE/bootstrap 零新依赖。

### 6.2 embedding provider

```python
class EmbeddingProvider(Protocol):
    def embed(self, texts) -> list[list[float]]
PROVIDERS = {"external": ExternalApiProvider, "local_bge": LocalBgeLazyProvider}
```

- **外部 provider 配置来源**：环境变量 `MODEL_DUEL_EMBEDDING_URL/KEY/NAME` 默认 + 页面可覆盖（存入评测配置，Key 不落盘）
- 外部 provider 走已验证的上游客户端（SSRF/审计复用）
- 本地 BGE 懒加载（首调才 import onnxruntime），未安装时降级报错并提示切 external

### 6.3 调度器

```python
class Scheduler:
    queue: PriorityQueue[(priority, enqueue_time, job_ref)]
    async def submit(job_or_batch, priority) -> queued   # 即建 sse_queue
    async def _dispatch():                                # 配额内派发；高优先级插队
    def cancel_queued(job_id)                             # 排队中移除 → cancelled
    def cancel_running(job_id)                            # 走现有 cancelling 语义
```

- 契约在迭代 0 定义；main.py 的 `MAX_ACTIVE_JOBS` 与 429 逻辑替换为调度器配额；`_jobs` 内存表保留为运行态视图
- **持久化决策**：v1 内存队列，进程重启时排队中任务置 `cancelled`（不恢复），避免队列持久化复杂度

### 6.4 评审接入点（main.py 重构）

- `_run_eval_impl` 作答完成分支按 review_mode 分叉（见 5.1）
- `StartRequest` 新增：`review: {mode, judge_config?, k_top_human?, fail_open?}`，缺省 `pure_human`（保持现状）

### 6.5 report_builder 扩展

- 指标分区（判别式/生成式）、KPI 区、元评估段（Spearman/Kappa）、环境快照段
- 排行榜模式：分维度排名表 + 综合分 + 胜率矩阵 + bootstrap CI 标注 + 多模型雷达 + 失败模型 N/A

## 7. API 设计（新增/修改）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/models` | 注册模型配置（Key 仅内存）；重名 400 |
| GET | `/api/models` | 列表（脱敏：`key_masked`/`has_key`，无 Key 明文） |
| GET | `/api/models/{id}` | 单条详情，**附带进程内存 Key**（供填入流程；缺失 404） |
| DELETE | `/api/models/{id}` | 删除配置并清内存 Key（缺失 404） |
| POST | `/api/eval` | StartRequest 扩展 review/gen 字段（兼容旧请求体） |
| POST | `/api/benchmark` | 创建 N 模型批次 |
| GET | `/api/benchmark/{batch_id}` | 批次状态/进度（多 job 聚合） |
| GET | `/api/benchmark/{batch_id}/leaderboard` | 排行榜报告（含 N/A 标注） |
| GET | `/api/stats/saturation` | 饱和度监测 |
| GET | `/api/datasets/{name}/export` | 评测包导出（zip） |
| POST | `/api/badcases/{id}/attribution` | 人工确认归因 |
| POST | `/api/datasets/{name}/review` | 出题审核提交（通过/驳回） |
| GET | `/api/tasks` | 任务队列视图（含优先级调整） |
| PUT | `/api/tasks/{job_id}/priority` | 改优先级（仅 queued 可改） |

SSE 事件扩展：`{"type":"queued"|"judging"|"batch_progress"|"budget_warning"|"judge_health"}`（复用 ticket 鉴权）。

## 8. 前端设计

| 页面 | 变更 |
|---|---|
| index.html 配置页 | 模型配置库选择（多选 N）、评审/出题策略配置、任务类型徽标 |
| 新增 任务列表页 | 队列/排队状态/取消/优先级调整（轮询兜底 + SSE） |
| 新增 审核页 | 出题列表/编辑/通过/驳回 |
| 新增 排行榜视图 | 分维度排名表、胜率矩阵、雷达图叠加、CI 标注、N/A 模型 |
| report.html | 指标分区、KPI、元评估、环境快照、bad case 入口、评测包导出 |
| review.html | 兼容混合模式（Top-K 复核提示） |

图表复用本地 ECharts：折线/箱线/散点/雷达/热力（胜率矩阵）。排队期间 SSE 未就绪时前端以轮询兜底。

## 9. 安全设计

1. **Key 不落盘**：模型配置库/辅助配置持久化仅 `key_masked`；内存表随进程销毁；导出/日志经 `redact_sensitive`
2. **出题内容安全**：生成题面视为不可信输入，入库前内容安全过滤（词表 + LLM 审核双保险）
3. **评审 prompt 注入**：题目/答卷文本拼接评审 prompt 前做围栏与长度限制；verdict 解析仅取结构化字段
4. **既有防护自动覆盖**：新接口统一走 access 中间件（Host/Origin/限流）；审计事件表扩展（benchmark_started、model_registered、attribution_confirmed 等）
5. **SSRF**：模型注册与 benchmark 启动即校验 URL（复用 `validate_upstream_url`）；本地 BGE 不经网络

## 10. 存储与兼容性

- `.eval/history/<jobId>/` 新增 `env.json`、`manifest.json`，既有文件不动；`_job_state` 向后兼容
- 数据集格式：新字段全部可选，`type` 缺省判别式 → 旧文件零破坏
- 跨 job 汇总/饱和度文件独立于 history，损坏时静默重建（幂等）
- 无迁移脚本需求（纯增量字段）

## 11. 测试策略

| 层 | 覆盖 |
|---|---|
| 纯函数单测 | metrics 各指标、embed 双 provider、extract 分层、stats（bootstrap/Kappa/Spearman/锚定偏移）、perturb 各模式、generator 校验链（含安全过滤拦截样例） |
| 集成（TestClient + mock 上游） | 评审接入后全链路状态机、混合评审提交、batch 聚合与 partial 语义、配置库 CRUD+脱敏、排队/取消/优先级（含排队取消分支）、断点续跑、饱和度汇总幂等 |
| 安全回归 | Key 不落盘（模型库/辅助配置）、评审 prompt 注入样例、审计事件白名单、SSE ticket 复用（沿用） |
| E2E（本地 playwright） | 配置库选模型→评测→排行榜视图 |

约定沿用：零真实网络、存储重定向临时目录、先回归后实现（红→绿）。

## 12. 性能与成本

- 执行单元：单模型串行逐题（保持现状），batch 内多模型按并发配额并行
- 预算预估：`预估 tokens = 题数 × 轮次 × N × (1 + 评审倍率)`，超限拒绝并提示；运行期 `budget_warning` 事件
- N 模型评审默认单臂（O(N)）；两两对决显式开启（O(N²)），配 K top 候选限制

## 13. 风险与开放问题

| 风险 | 缓解 |
|---|---|
| main.py 重构与调度器引入回归 | 迭代 0 契约 + 行为等价测试（现全部集成测试保持绿） |
| 评审接入改变既有人工流程 | review_mode 默认 pure_human，Agent 显式开启 |
| 本地 embedding 依赖不可装 | external provider 默认（环境变量 + 页面覆盖）；懒加载失败仅语义指标降级 |
| 排行榜样本量不足误导 | bootstrap CI + 「差异不显著」标注 + 最小题数提示 |
| 多 job 并发下内存/SSE 增长 | 队列视图分页、运行态只留必要字段、终态 job 从内存卸载 |
| 排队任务随重启丢失 | v1 内存队列，重启置 cancelled（文档化行为，前端提示） |
| 出题/embedding 配置来源混乱 | 统一「辅助模型配置」段（gen/judge/embedding），API 显式传参 + 环境变量默认 |
