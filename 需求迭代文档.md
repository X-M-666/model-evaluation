# 模型评测平台 · 需求迭代文档

## 1. 文档说明

### 1.1 项目背景
输入模型 API 配置，自动完成「出题 → 作答 → 评审 → 报告」的评测流程。现为**双模型对决**形态，本次演进目标是：**给定 N 个模型，一键产出横向 benchmark（排行榜）**。

### 1.2 目标能力
1. 判别式 / 生成式双任务类型评测（prompt + rubric，而非仅 expected 比对）
2. 八维能力体系（含安全与价值观，单独呈现不计总分）
3. 评测结果可信：客观指标 + 统计显著性 + 评审元评估 + 人工复核
4. 评审 = 第三方 Agent + 人工共同执行，可配置
5. LLM 出题 + 人工审核 + 评测集版本化
6. Bad Case 全链路闭环（分类/诊断/挖掘/可视化/反馈）
7. 对抗鲁棒性与偏见公平性测试
8. 多任务排队、优先级调度、CPU 池化、断点续跑
9. N 模型横向 benchmark：排行榜报告形态
10. 持续回归测试与安全基线不变量

### 1.3 非目标（明确不做）
- 多模态评测（文本/图像/音视频输入）
- 多轮对话 / Agent 交互评测

## 2. 决策基线

| 决策项 | 结论 |
|---|---|
| 节奏 | 里程碑驱动，按内容完成度推进，不绑定工期 |
| 模态范围 | 纯文本评测 |
| 任务类型 | 判别式 / 生成式一等公民 |
| 语义相似度 | embedding 双 provider（默认本地 BGE 可懒加载，可切外部 API，缺省离线可用） |
| 依赖策略 | 默认零新依赖路线（BLEU/ROUGE、bootstrap CI 纯 Python 实现） |
| 资源管理 | CPU 并发池化落地，GPU 仅预留抽象接口 |
| 评审接入 | AI 评审当前未接入主流程（纯人工），**评审接入前置到迭代 2** |
| 调度实施 | 迭代 7 **串行**（迭代 6 之后），接口契约在迭代 0 确定 |
| 可靠性功能 | P0+P1+P2 全纳入 |
| N 模型评审 | 默认单臂参考评分（O(N)），两两对决（O(N²)）仅 top 候选可选 |

## 3. 评测维度体系（八维）

| # | 维度 | 与现状关系 | 计分 |
|---|---|---|---|
| 1 | 知识能力 | 保留（吸收"简单理解"） | ✓ |
| 2 | 数学能力 | 新增（从推理拆分） | ✓ |
| 3 | 逻辑推理能力 | 保留（仅推理，去数学） | ✓ |
| 4 | 代码能力 | 保留 | ✓ |
| 5 | 语言能力 | 保留（承载生成式任务） | ✓ |
| 6 | 指令遵循与对齐 | 保留 | ✓ |
| 7 | 安全与价值观 | 新增 | **不计入总分**，报告单独展示 |
| 8 | 长文本与效率稳定性 | 合并改名（原"长文本与多模态"+"效率与稳定性"） | ✓ |

**权重**：推理/数学/代码占比偏大；安全维单独打分不参与排名。
**影响**：`executor.py:108-109`、`tasks.py:265/300`、`report_builder.py:603` 魔法字符串改引用 `DIMENSIONS` 常量；历史记录不受影响（维度为自由字符串）。

## 4. 任务模型（schema 定义）

```json
{
  "id": "T1",
  "type": "判别式|生成式",
  "dimension": "（八维之一）",
  "difficulty": "easy|medium|hard",
  "tags": ["可选标签"],
  "prompt": "题目内容",
  "expected": "判别式必填；生成式可选",
  "rubric_note": "生成式必填（评分依据）",
  "context": "可选，附加上下文/参考文档，供 RAG 忠实性评测",
  "test_cases": "可选（代码题）"
}
```

关键点：`context` 字段是迭代 4 RAG/grounding 评测的载体，必须在迭代 1 定义，否则后续无法落地。

## 5. 迭代计划

依赖链：`迭代1 → 迭代2 → 迭代3 → 迭代4 → 迭代5 → 迭代6 → 迭代7（串行）→ 迭代8`

### 迭代 0：工程质量基线（横切，贯穿全程）
**目标**：确立全周期不变量与接口契约。
- CI 常态化回归：每迭代验收强制「先回归测试 → 实现 → 全绿」
- 立扩展点：metrics 注册表、任务类型注册表、**跨 job 历史汇总接口**（迭代 5 饱和度监测的隐藏前置）
- **立调度器接口契约**（队列/优先级/并发配额），供迭代 7 串行落地
- 安全基线：Key 不落盘、SSRF、鉴权/审计/限流对新接口自动接入
- **交付物**：扩展点接口定义文档 + 调度器契约 + CI 全绿

### 迭代 1：任务模型与评测集地基
**目标**：schema 与八维体系落地，评测集/模型配置可入库。
- `datasets.py`：task 新增 `type/difficulty/tags/context`（含生成式 rubric 必填约束）
- `parsers.py` 四格式解析器支持新字段（缺省=判别式，向后兼容）
- **八维体系落地**：`DIMENSIONS` 重定义、魔法字符串清理、题库池重配（数学/安全新增题，安全题按出题指导原则编写，避免诱导有害输出）
- **模型配置库**：N 套配置本地保存（URL/名称/参数，Key 不落盘），增删改查 + 页面选择
- 存储层扩展：评测集版本化元信息 + 跨 job 历史汇总接口
- 前端：上传/预览任务类型徽标；模型配置库页面
- **验收**：混合题型含 context 评测集完整解析；老格式零破坏；配置库增删改查通过；跨 job 汇总接口 mock 数据可用
- **交付物**：schema 变更、八维题库、模型配置库 API + 页面

### 迭代 2：指标引擎、统计体系与 AI 评审接入
**目标**：能力评价落到可计算指标；AI 评审接入主流程（迭代 3 前置）。
- `metrics.py` 按类型分支：判别式（Top-1/F1/精确匹配/松弛准确率）；生成式（语义相似度双 provider/一致性/BLEU/ROUGE）
- 答案提取分层：规则匹配 → LLM 解析兜底
- 提示模板执行侧：直答 / Zero-shot CoT / Few-shot 注入
- 统计显著性：胜率、bootstrap CI、「差异不显著」提示
- 截断/无效作答 → 评审提示 + 报告警告；预算熔断（N 模型批次预估放大）
- **AI 评审接入**：`StartRequest` 新增 judge 配置，`judge.py` 接入 `main.py` 主流程（作答→自动评审→报告）
- KPI 结构化采集；报告按判别式/生成式分区 + KPI 区
- **验收**：指标纯函数全绿；embedding provider 可切换缺省离线；全链路（作答→评审→报告）mock 跑通
- **交付物**：metrics 模块、评审接入、报告指标区

### 迭代 3：多评审方升级 + 评审可靠性工程
**目标**：评审多方共同把关且可被度量。
- 评审方抽象：第三方评审 Agent（独立配置）＋ 人工评审（复用双盲）
- 评审策略：纯 Agent / 纯人工 / 混合加权可配置
- **N 模型评审模式**：单臂参考评分（rubric+金标锚定，O(N)，默认）/ 两两对决（O(N²)，top 候选）
- 生成式 rubric 评审打分协议（结构化输出）
- **逐题独立随机交换 X/Y** + 每题 reveal 映射（消除位置偏差）
- **金标校准集 + 元评估**：人工权威评分金标题（20~30 题，**人工前置资源**）→ Spearman 相关性、Cohen's Kappa
- 重点案例人工复核（按评分/分歧度筛选 Top 案例）
- 评审健康度：invalid 率阈值告警、失败可配置降级人工
- **验收**：三种策略切换跑通；元评估指标随报告展示；复评一致率可见
- **交付物**：评审方抽象、金标集、元评估报告段

### 迭代 4：LLM 出题与内容质量
**目标**：评测集规模化生成、质量可控、可追溯可复现。
- 出题 pipeline：分类型/维度模板 → 生成 → 自动校验（去重/可解性/rubric 完整性/防泄漏/**内容安全过滤**）→ 待审核入库
- 出题模板带 CoT 配置；人工审核页（编辑/通过/驳回，**人工前置资源**）
- 评测集版本化（历史评测引用版本快照）
- RAG/上下文忠实性评测（`context` + 问题，测 Faithfulness/Answer Relevancy，不需真实检索）
- 评测环境快照（种子/温度/模型名/评审配置指纹）；可复现评测包导出（题目+答卷+配置+种子）
- **验收**：生成→审核→入库→评测全链路；安全过滤拦截注入样例
- **交付物**：出题 pipeline、审核页、RAG 评测集模板、评测包导出

### 迭代 5：Bad Case 体系与基准饱和度
**目标**：错误沉淀为资产并驱动题库演进。
- 错误分类五类：事实/语义/推理/安全偏见/执行；LLM 自动归因打标 + 人工抽验
- 自动挖掘：低分、双败、评审分歧、边缘情境样本入池
- 可视化：bad case 列表、分类分布、错误类别分布图、归因报告
- 反馈闭环：bad case → 题库修订建议 → 回归重测
- **基准饱和度监测**：基于迭代 0 的跨 job 历史汇总，通过率上升趋势 → 提示题库过时（无历史数据时空态）
- **验收**：一次评测自动产出 bad case 库；饱和度监测空态/有数据态均正确
- **交付物**：bad case 库、归因服务、饱和度监测

### 迭代 6：对抗鲁棒性、偏见测试与高级可视化
**目标**：压力测试能力与 N 模型排行榜报告。
- 对抗样本生成（改写/扰动/噪声注入）与鲁棒性衰减曲线（扰动强度→得分）
- 偏见与公平性测试（性别/地域/文化属性扰动对照，复用扰动管线）
- 图表：多折线（K-召回率）、箱线图、散点图（得分-延迟/Token）
- KPI 看板（耗时/token 趋势/硬件利用率 CPU，GPU 无硬件时 N/A）
- **N 模型排行榜**：分维度排名表 + 综合分 + 胜率矩阵 + bootstrap CI 标注，替代双模型 winner/loser 结论；雷达图多模型叠加
- **验收**：图表齐全；3+ 模型排行榜演示数据可用
- **交付物**：扰动管线、排行榜报告形态

### 迭代 7：资源管理与任务调度（串行，迭代 6 之后）
**目标**：多任务并发可控、可续跑，支撑 benchmark 批次。
- 调度器按迭代 0 契约落地：队列、优先级、并发配额（替代固定并发 2）
- **benchmark 批次**：1 任务集 × N 模型 × M 轮，每模型一个执行单元
- CPU 池化弹性分配；GPU 接口预留；硬件利用率采集
- 断点续跑：瞬时结果增量落盘，API 故障重试失败题
- 前端：任务列表、排队状态、取消、优先级调整
- **验收**：排队/抢占/取消演示；调度与续跑单测全绿；N=5 benchmark 批次跑通
- **交付物**：调度器、benchmark 批次 API、任务管理页

### 迭代 8：整体打磨与全量回归
- 配置页/报告页/评审页/新页面（配置库/审核/任务列表/排行榜）布局统一，空态/加载态/错误态完善
- 全量回归 + 性能基准（长文本、多任务并发、N 模型批次）
- 用户手册与演示脚本（含 N 模型 benchmark 演示）
- **验收**：全量测试绿；无已知体验缺陷
- **交付物**：完整产品 + 手册 + 演示

## 6. 横切保障

| 横切项 | 执行方式 |
|---|---|
| 回归测试 | 每迭代「先回归 → 实现 → 全绿」；CI 三平台矩阵 |
| 安全基线 | 鉴权/审计/SSRF/限流自动接入；Key 不落盘不变量 |
| 兼容性 | 数据集/历史记录格式变更向后兼容 |
| 人工资源 | 金标校准集（迭代 3）与出题审核（迭代 4）为前置人工依赖，须提前安排 |

## 7. 风险与缓解

| 风险 | 缓解 |
|---|---|
| N 模型评测成本（题目×N×M×评审） | 默认单臂评审 O(N)、预算熔断、两两对决仅 top 候选 |
| 本地 embedding 依赖过重 | 外部 API provider 默认、本地 BGE 懒加载不进 CI 默认矩阵 |
| 调度与主线改造冲突 | 迭代 0 立契约 + 迭代 7 串行 |
| 出题质量/安全 | 自动校验 + 内容安全过滤 + 人工审核把关 |
| 排行榜可信度 | bootstrap CI + 「差异不显著」标注 |
| 历史数据不足（饱和度监测） | 空态设计，随数据积累生效 |
