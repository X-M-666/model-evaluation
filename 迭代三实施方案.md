# 模型评测平台 · 迭代三实施方案（完整定稿）

> 对应《迭代计划》迭代 3（多评审方升级 + 评审可靠性工程）、《技术设计文档》§4.2/§5.2/§6.4/§7。本文记录范围决策（含全局评审修正 H1~H4、M1~M5、L1~L3）、模块设计、API 契约、测试计划与实施顺序。基线：迭代二完成态（609 passed, 30 skipped）。

## 一、范围与决策确认

### 1.1 范围总表

| 事项 | 本轮落地 | 说明 |
|---|---|---|
| 评审方抽象 `reviewer.py` | ✅ | Reviewer 协议 + AgentReviewer（包装 judge）+ HumanReviewer（薄封装），不做 plugin 机制 |
| 评审策略 `pure_agent / pure_human / hybrid` | ✅ | hybrid = Agent 预评 + 分歧/低分 Top-K 转人工复核，**人工分覆盖**被选题 |
| 单臂 rubric 评审协议 | ⚠️ 协议层先行 | `run_single_arm_judge` 纯协议 + 单测；**不接主流程**（迭代 7 benchmark 接线）；两两对决 = 现有 run_judge |
| 金标锚定 | ⚠️ 协议层先行 | `calibration_offset` 纯函数 + 金标集存储/API/元评估；偏移仅随元评估展示，**本轮不自动校正评审分** |
| 生成式 rubric 结构化打分协议 | ✅ | 单臂版 prompt + 独立结构校验（双盲版迭代二已有） |
| 逐题独立随机交换 X/Y | ✅ 仅 agent | 题级 reveal 向后兼容扩展 + 按题归一化跨模块改造（H1）；人工评审保持轮级 |
| 金标校准集 + 元评估 | ✅ 机制先行 | Spearman / Cohen's Kappa / 锚定偏移；demo 金标（4~6 题×2 模型）跑通全链路，真实金标经 API 覆盖 |
| 重点案例人工复核 | ✅ | hybrid Top-K：invalid 必选 → 分差降序 → 低分兜底（H2） |
| 评审健康度 | ✅ | invalid 率 >10% 阈值告警（judge_health 扩展）；fail_open 降级沿用，hybrid 下降级退化为纯人工（H3） |
| 复评一致率 | ✅ | repeat_n≥2 时稳定空间逐轮 winner 一致率（M4） |
| N 模型 batch / 排行榜 | ❌ | 迭代 7（调度器） |
| 人工评审 reveal 改题级 | ❌ | 决策：仅 agent |
| ReviewConfig.arm 字段 | ❌ | 不加死字段（L2）；单臂能力由函数协议表达 |

### 1.2 用户已拍板决策

1. 单臂评审仅做协议层，不改双模型 agent 评审形态
2. hybrid 融合 = 人工分覆盖（复核题人工为准，未复核沿用 agent 分）
3. 金标集机制先行 + demo 金标；真实金标后续人工录入（manual 覆盖 demo）
4. 逐题独立随机交换仅 agent 评审；人工评审保持轮级

### 1.3 关键设计定案

- **题级 reveal 向后兼容**：结构扩展为 `{"rounds":[...](轮级兜底), "per_task":[{"task_id","answer_x","answer_y"}...]}`；`_normalize_reveal` 支持按题查找；旧数据（仅 rounds）回退轮级，零破坏
- **题级 reveal 数据流（H1）**：`make_task_reveal`（judge.py）→ 落盘 reveal.json → agent 逐轮评审 `_round_verdict_from_judge`（main.py）把每题 reveal 写入该题 score 上下文 → 跨轮聚合 `human_review._stable_scores`（human_review.py）按题查 per_task 归一化，无 per_task 时回退轮级 → 对旧数据/人工评审路径行为不变
- **hybrid 复用既有评审闭环**：agent 预评产物（round_verdicts）已与人工同构 → 复核提交按题覆盖分数即可；状态机不增节点（judging → reviewing 复用）
- **hybrid 提交独立端点**：不放宽纯人工 `_validate_review_scores` 全量校验语义；hybrid 子集校验独立实现
- **失败降级（H3）**：hybrid 下 agent 全败 fail_open=True → 退化为纯人工（无复核集、全量提交走 `/review`、review_mode 落盘 pure_human + 降级标注）
- **重启恢复（M2）**：hybrid-review.json + round-verdicts.json 落盘后，独立端点复用 `_load_job_state` 磁盘恢复，重启后复核可继续提交（与纯人工一致）
- **元评估口径（M5）**：多轮时金标匹配取**最后一轮** reveal 投影的评审分

## 二、模块设计

### 2.1 `stats.py`（扩展，纯函数）

- `spearman(x, y) -> float`：秩相关（并列取平均秩），零依赖
- `cohen_kappa(labels_a, labels_b, categories=None) -> float`：类别一致性（评分分档或 winner 三分类）
- `calibration_offset(gold_scores, judge_scores) -> float`：金标锚定线性偏移 `= mean(gold) - mean(judge)`
- `consistency_rate(round_winners: list[str], categories=("model_a","tie","model_b")) -> float`：稳定空间逐轮 winner 一致率（M4 口径：调用方须先归一化到稳定模型名）
- 全部确定性可测；调用方按 scoring_ids 过滤约定沿用

### 2.2 `judge.py`（扩展）

- `make_task_reveal(task_ids, seed=None) -> {"rounds": [...], "per_task": [...]}`：逐题独立随机交换
- `_normalize_reveal` 扩展：`per_task` 按 task_id 查映射（缺省回退轮级/随机）
- `run_single_arm_judge(task_set, answers, judge_config, progress_cb=None) -> dict`：单臂 rubric 评分协议——每题单答案结构化输出 `{"id","dimension","score","basis","_invalid"}`；复用 `_call_judge_model`/`_fenced`/`_parse_verdict`（仅提取，**结构校验独立实现**，L1）；输出 scores + totals + meta + health；**无 gold_offset 死参数**（M1，偏移仅元评估展示）
- `_build_single_arm_prompt(task, answer)`：题 + rubric_note + 单答案 + 结构化打分协议
- `_parse_single_arm_verdict(raw) -> dict | None`：结构校验（score 数值 0-10、basis 非空）
- `health_check(meta, threshold=0.1) -> {"healthy","invalid_rate","alarm"}`：invalid 率阈值判定
- **不接主流程**（协议层），`_run_agent_judging` 保持 pairwise

### 2.3 `human_review.py`（修改，H1 核心）

- `_stable_scores(round_verdict, per_task_reveal=None)`：新增可选 `per_task_reveal` 参数——按题查 answer_x_file 归一化；无 per_task 时回退轮级（现状不变）
- `_stable_names(last, per_task_reveal=None)`：配套按题取模型名
- `build_final_verdict(round_verdicts, repeat_n, per_task_reveal=None)`：透传；旧调用零破坏
- `make_reveal`/`resolve_round` 不动（人工评审轮级语义不变）

### 2.4 `reviewer.py`（新增）

```python
class Reviewer(Protocol):
    async def review_all(task_set, answers, config) -> dict

class AgentReviewer:   # 包装 judge.py（pairwise→run_judge / single→run_single_arm_judge）
class HumanReviewer:   # 薄封装 human_review 提交语义（本轮提供协议+单测，main 不引入分发）
```

### 2.5 `schemas.py`（修改，全可选向后兼容）

```python
class ReviewConfig(BaseModel):
    mode: str = "pure_human"            # pure_human | pure_agent | hybrid
    judge: ModelConfig | None = None
    fail_open: bool = False
    k_top_human: int = Field(5, ge=1, le=20)   # hybrid 复核题数

class GoldScoreItem(BaseModel):
    task_id: str; model_name: str; score: float; note: str = ""

class GoldSetRequest(BaseModel):
    name: str; items: list[GoldScoreItem]
```

### 2.6 `main.py`（hybrid 接入 + 健康度 + 视图扩展）

- **hybrid 状态流**：`executing → judging（agent 全量预评）→ reviewing（复核态）→ completed`
  - `_run_agent_judging` 完成 → 选择复核集（H2）：**invalid 题必选** → 其余按分差 |x−y| 降序 → 低分兜底；`k = min(k_top_human, 候选数, 总题数)`（L3）；`k==0` 直通 completed（不 reviewing）
  - 落盘 `hybrid-review.json`：`{"k", "review_set":[{round, task_id, agent_x, agent_y, winner, basis}], "selected_at"}`
  - 进入 reviewing；SSE `{"state":"reviewing","mode":"hybrid","k":K}`
  - fail_open 降级（H3）：退化为纯人工，review_mode 落盘 `pure_human` + 降级标注，无复核集
- **复核提交** `POST /api/eval/{id}/hybrid-review`：
  - 非 hybrid / 未进复核态 / 已提交 → 409；子集校验：round∈1..repeat_n、task ∈ 复核集、每 (round,task) 恰一次 → 否则 400
  - `merge_hybrid_verdicts(round_verdicts, human_scores)` 纯函数按题覆盖（score/winner/basis，人工为准）→ `build_final_verdict` → `_finalize_job`（review_data 含 mode=hybrid）
  - 重启恢复（M2）：复用 `_load_job_state` + `load_round_verdicts` 相关磁盘数据
- **健康度告警**：judging 结束后 `health_check(meta)` 不健康 → SSE `judge_health {status:"invalid_rate", rate, threshold}`（幂等单次）；全 invalid 仍走既有降级
- **视图扩展（H4）**：`GET /api/eval/{id}/review` 对 hybrid 任务返回 `hybrid: {review_set, agent_scores, k}`（纯人工任务缺省，兼容）
- `start_eval`：mode=hybrid 时 judge 必填（同 pure_agent 校验）
- agent 评审在 remaining 期（**逐题 reveal 生效**）：`_run_agent_judging` 改用每轮 `make_task_reveal` → reveal.json 落盘 per_task → `_round_verdict_from_judge` 逐题 revealed → `build_final_verdict(..., per_task_reveal)` → `_stable_scores` 按题归一化
- audit：`eval_judged`（补迭代二缺口）、`gold_added`

### 2.7 金标集（存储 + API）

- 存储：`webapp/data/gold/<safe_name>.json`
  `{"name","items":[{"task_id","model_name","score","note"}],"source":"demo|manual","created_at"}`
- 启动时 gold 目录为空 → 载入 demo 金标（内置题库真实 id：生成式题 4~6 题 × 2 模型；**人工编写近似权威分**，`source:"demo"` 明确标注，M3）；manual 录入同名覆盖（source=manual）
- API：`GET /api/gold` 列表（含 source）；`POST /api/gold` 录入/覆盖；`GET /api/gold/{name}/meta-eval?job_id=`（任务未完成 404；金标与 job 题不匹配 → 空态）
- 题库 id 调整导致失配 → meta_eval 空态兜底（不报错）

### 2.8 `report_builder.py`（扩展）

- 新增 `meta_eval` 段：`{"available", "spearman", "kappa", "gold_offset", "gold_source", "matched", "gold_total", "note"}`（空态 note 提示演示金标/样本不足）
- 新增 `consistency`：repeat_n≥2 时稳定空间逐轮 winner 一致率（M4），单轮 null
- `judge_mode` 扩展输出 hybrid；`k_top_human` 入 summary；`review_mode` 降级标注（H3）
- 既有字段不动，前端兼容渲染

### 2.9 `storage.py` / `audit.py`

- `save_gold / load_gold / list_gold / delete_gold`（data/gold/）
- `save_hybrid_review / load_hybrid_review`（job 目录 hybrid-review.json）
- `get_job_files` 白名单加 `hybrid-review.json`
- audit：`gold_added`、`eval_judged`

### 2.10 前端

- **index.html**：评审面板模式加 `hybrid`（K 输入）；不加 arm 选项（L2）
- **review.html**：hybrid 复核态——仅渲染复核题（agent 原分只读参考 + "复核提交后将覆盖 AI 分"提示）；提交走 hybrid-review 端点；纯人工态不变
- **report.html**：元评估段（Spearman/Kappa/偏移/demo·manual 标注、空态文案）、复评一致率、hybrid 徽标 + K 值
- SSE：judge_health `invalid_rate` 分支提示

## 三、API 契约变更

| 方法 | 路径 | 变更 |
|---|---|---|
| POST | `/api/eval/start` | ReviewConfig 扩展 mode=hybrid / k_top_human（旧请求体零破坏） |
| POST | `/api/eval/{id}/hybrid-review` | 复核子集提交（校验 ⊆ 复核集；非 hybrid/未进复核态/已提交 409） |
| GET | `/api/eval/{id}/review` | hybrid 任务返回 `hybrid` 字段（复核集+agent 原分；纯人工缺省兼容） |
| GET | `/api/gold` / POST `/api/gold` | 金标集列表 / 录入覆盖（demo 可被 manual 覆盖） |
| GET | `/api/gold/{name}/meta-eval` | 对指定 job 的元评估（未完成 404；不匹配空态） |
| SSE | `/events` | judge_health 扩展 `status:"invalid_rate"`；reviewing 事件带 mode/k |
| GET | `/api/eval/{id}/report` | report 新增 meta_eval / consistency；judge_mode 含 hybrid |

## 四、测试计划（先回归 → 实现 → 全绿）

### 4.1 前置（第 0 步）

1. 提交迭代二工作区全部改动（含审查修复 3 处）
2. 全量回归基线：609 passed, 30 skipped 全绿后固化

### 4.2 新增测试文件（6 个）

| 文件 | 覆盖 |
|---|---|
| `test_stats_ext.py` | spearman（单调/并列秩）、cohen_kappa（一致=1/随机≈0/类别集合）、calibration_offset、consistency_rate |
| `test_single_arm.py` | 单臂 prompt、结构校验（非法 score 拒绝）、invalid 标记、health_check 阈值、progress_cb、零网络 |
| `test_task_reveal.py` | make_task_reveal 逐题独立；`_stable_scores` 按题归一化（human_review 跨模块）；旧 rounds-only 回退；build_final_verdict per_task 透传 |
| `test_hybrid.py` | judging→reviewing（invalid 必选/排序）；子集提交→覆盖融合→completed；非 hybrid 409；越界 400；k==0 直通；fail_open 退化纯人工（H3）；**重启恢复**（M2）；SSE 序列 |
| `test_gold.py` | 金标 CRUD/demo 自动载入/manual 覆盖；meta-eval 计算与空态；报告 meta_eval 段 |
| `test_judge_health.py` | invalid 率超阈值 → invalid_rate 告警事件（幂等）；未超无告警 |

### 4.3 修改既有测试

- `test_schemas` / `test_agent_judge` / `test_judge`：ReviewConfig 新字段、reveal per_task 注入与回退、`_normalize_reveal` 扩展分支、`_stable_scores` 签名兼容

### 4.4 验收映射

1. 三种策略切换跑通 → test_hybrid + 既有 pure_agent/pure_human 全绿
2. 元评估指标随报告展示 → test_gold + report meta_eval 段
3. 复评一致率可见 → consistency 段（repeat_n=3 用例）
4. 健康度阈值告警 + 可配置降级 → test_judge_health + 既有 fail_open 用例

## 五、实施顺序

1. 第 0 步：落盘本方案 + 提交迭代二 + 全量回归基线
2. stats.py 扩展 + 单测（test_stats_ext）
3. judge.py 单臂/逐题 reveal/健康度 + human_review 按题归一化 + 单测（test_single_arm / test_task_reveal / test_judge_health）
4. schemas + reviewer.py + 单测
5. 金标集存储/API/demo/audit + 单测（test_gold）
6. report_builder meta_eval/consistency + 单测更新
7. main.py hybrid 状态流/融合/健康度/视图扩展 + test_hybrid 集成
8. 前端 index / review / report
9. 全量回归 + 验收三要素汇报

## 六、风险与缓解

| 风险 | 缓解 |
|---|---|
| 逐题 reveal 破坏聚合归一化（H1） | per_task 向后兼容 + human_review 按题查找 + 旧轮级测试保持绿 |
| hybrid 子集校验回归面 | 独立端点 + 独立校验，不动纯人工全量校验 |
| demo 金标误导 | source 标注 + 报告"演示金标"提示 + manual 覆盖机制 |
| 元评估样本不足 | 空态 + 样本数标注 + 题库失配兜底 |
| 单臂协议无人消费 | 协议层定位 + 迭代 7 接线点注释；不接 main 避免死分支 |
| 健康度告警刷屏 | 幂等单次告警（沿用 budget_warning 模式） |
| hybrid 进行中崩溃 | 复核集/轮次 verdict 均落盘，重启可续提交（M2） |