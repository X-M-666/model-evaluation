# 迭代四验收报告：LLM 出题与内容质量

> 基线 commit `5b060c8`（迭代三）| 全量回归 **781 passed / 30 skipped**（0 失败）
> 测试命令：`cd webapp && python -m pytest tests -q --tb=short -p no:cacheprovider`

## 一、验收结论

| 验收项 | 结论 | 证据 |
| --- | --- | --- |
| 全量回归零失败 | ✅ | 781 passed / 30 skipped（基线 708/30 + 新增 73 用例） |
| 新增用例全绿 | ✅ | 23(generator) + 17(generate_api) + 8(context) + 10(grounding) + 5(rag_demo) + 5(env) + 5(export) = 73 |
| 零真实网络 | ✅ | conftest 全局封锁，新增测试均 mock 上游 |
| 零密钥落盘 | ✅ | 批次 spec 仅 `gen_name`/`gen_key_masked`；config/env/报告均掩码 |
| 浏览器安全回归 | ✅ | test_xss_playwright 9 passed（含新页面 CSP nonce） |

## 二、功能验收明细

### 2.1 LLM 出题 pipeline（生成 → 五级校验 → 待审核批次）

- `backend/engine/generator.py`：8 维度 × 2 题型 × with_context/cot/fewshot 模板；`_parse_gen_output` 白名单解析；五级校验链：
  - `check_dedup`（与目标评测集去重，阈值 0.85）
  - `check_leakage`（答案泄漏入题面）
  - `check_rubric`（生成式必带 rubric_note）
  - `check_solvable`（判别式自答可解性；失败降级 skipped/需人工复核，不拦截）
  - `check_safety_static` + `check_safety_llm`（安全维度 → `manual_required`）
- 出题异步化：`gen_<id>` 独立协程槽（与评测任务共池，上限 3）；批次状态 `generating → ready|partial|error`；进程中断遗留批次沉降 `partial`（`_settle_generation`）。
- API：`POST/GET /api/generate`、`GET /api/generate/{gen_id}`、`POST /api/generate/{gen_id}/items/{item_id}/review`。

### 2.2 人工审核与评测集版本化（E1/E5/E6/E7）

- approve 可选 edits（字段白名单 `EDIT_ALLOWED_FIELDS`，越界 400）；单题校验走 `validate_standard_dataset` 整集路径（E1）。
- 追加入库：`MAX_DATASET_TASKS=200` 上限（超限 400，E5）；`bump_dataset_version` 版本递增（v0→v1 起始，E6 附带修复：单题校验自动 id 与既有题目冲突 → 按现有 id 扫描最小空闲号重排，修复了第二次审核必撞 T1 的 bug）。
- reject 为终态（再审核 409，E7）；非 pending 409；非法 gen_id/item 400/404。
- 审计事件：`task_generate_started` / `task_reviewed`（含 action 路径）。

### 2.3 context 注入（D3）

- `executor.build_prompt`：任一策略（direct/cot/fewshot）下 context 均以「【参考文档】…【任务】」块置于题面前。
- 评审侧：`judge._build_blind_prompt` / `_build_single_arm_prompt` 追加参考文档段；`human_review.build_review_view` 透出 context；review.html 渲染参考文档块。
- 验证：`test_context_inject.py`（8 用例）。

### 2.4 RAG / grounding 指标（D4）

- 纯 n-gram 零依赖：`grounding_faithfulness`（答案 vs 参考文档 cosine）、`answer_relevancy`（答案 vs 题面）；阈值 `GROUNDING_SUPPORT_THRESHOLD=0.35`。
- 报告 `metrics.grounding` 段：仅当任务集存在带 context 的题目时出现；逐题 X/Y 侧 faithfulness/answer_relevancy/grounded 判定；无答案侧 `grounded=False + reason=no_answer`。
- 验证：`test_grounding.py`（10 用例，含 build_report 打包路径）。

### 2.5 内置 RAG 演示集（D2）

- `backend/engine/rag_demo.py`：`ensure_demo_rag_dataset()` 于 lifespan 种子化，`source="builtin_demo"`，幂等（用户上传覆盖不回退）；3 道生成式题均携带 300~500 字参考文档。
- 验证：`test_rag_demo.py`（5 用例，含 TestClient 生命周期触发）。

### 2.6 环境快照（env.json）

- `storage.save_env_snapshot` 于 `start_eval` 落盘 `<job>/env.json`（OS/Python/CPU/cwd/关键依赖版本，无密钥）；`get_job_files` 白名单新增 env.json；报告内嵌 `env_snapshot` 段（内存/磁盘两路径一致，`test_report_identical_memory_vs_disk` 回归通过——移除快照内时间戳保证可复现一致性）。
- 验证：`test_env_snapshot.py`（5 用例）。

### 2.7 评测包导出（E2）

- `GET /api/eval/{job_id}/export`：仅 completed 可导出（否则 409）；非法/不存在 404；zip 白名单（tasks/answers-a|b/verdict/report/config/round-verdicts/hybrid-review/env/answers-*-r*）+ `MANIFEST.json`（逐文件 sha256）；审计 `dataset_exported`。
- 验证：`test_export.py`（5 用例，含 zip 魔数、sha256 逐文件核验、无密钥断言）。

### 2.8 前端

- index.html：出题面板（维度/题型/数量/目标评测集/CoT/context 选项、一键复用评审模型）+ 待审核批次列表（状态徽标 + 审核入口）。
- `gen_review.html`（新页面，CSP nonce 托管）：逐题展示题面/参考文档/五级校验徽标 + 可编辑字段 + 批准/驳回。
- review.html：参考文档渲染（评审可见）。
- report.html：RAG 忠实性段（忠实度/相关性/有据判定表）+「导出评测包」按钮。

## 三、实现中的问题修复记录

| 问题 | 修复 |
| --- | --- |
| 测试 `_settle` 同步调协程 → 16 用例 TypeError | 抽取 `_call` 统一 `asyncio.run` 包装路由函数 |
| 路由全为 async，测试直调得到 coroutine | 同上（`get_job_files` 为同步、直接调用） |
| 单题校验每次分配 id=T1，第二次审核必撞 | main.py 追加前按现有 id 扫描最小空闲号重排 |
| 报告内存/磁盘两路径 env_snapshot 不一致 | 快照内容去掉 created_at，改读同一 env.json，内存路径同步挂载 |
| rag_demo T3 文档 299 字不足 300 | 补一句扩展至 300+ |
| 出题批次测试与既有用例共享数据集导致重复 id | 全链路用例改用独立评测集名 |
| 审计日志跨用例累积 → 断言 6≠2 | autouse fixture 按用例清空 audit 文件 |
| xss_playwright 断言 `.ds-item` first 被 rag_demo 抢占 | 改为 `has_text` 定位目标项 |
| 前端冒烟 | TestClient 断言新页面/新按钮/非ce 注入存在 |

## 四、遗留与说明

- 出题模型真实调用（非 mock）需部署方提供 API Key，测试全部走 fake pipeline（与既有 mock 约定一致）。
- `gen_review.html` 的编辑字段为「提交时合并」模型，前端展示可编辑但保存即入库；未做草稿持久化（批次本身即持久化草稿）。
- 导出包不含 API Key（config.json 已掩码）；MANIFEST 提供完整性校验入口，客户端解包可自行核验。
- 根目录未跟踪文件（image_monitor_dataset.json 等）为演示产物，未纳入提交范围。

## 五、建议后续（迭代五候选）

- 出题批次批量操作（整批通过/按维度批量驳回）、批次重跑生成。
- grounding 指标引入 LLM 判断层（当前为纯 n-gram，忠实性语义上限受限）。
- 导出包支持「含答案原文 vs 脱敏」两种级别。
