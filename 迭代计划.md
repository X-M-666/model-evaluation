# 模型评测平台 · 迭代计划

## 一、迭代计划总览

### 评测维度体系（八维）

| # | 维度 | 与现状关系 | 计分 |
|---|---|---|---|
| 1 | 知识能力 | 保留（吸收"简单理解"） | ✓ |
| 2 | 数学能力 | 新增（从推理拆分） | ✓ |
| 3 | 逻辑推理能力 | 保留（仅推理，去数学） | ✓ |
| 4 | 代码能力 | 保留 | ✓ |
| 5 | 语言能力 | 保留（承载生成式任务） | ✓ |
| 6 | 指令遵循与对齐 | 保留 | ✓ |
| 7 | 安全与价值观 | 新增 | **不计入总分**，报告单独展示 |
| 8 | 长文本与效率稳定性 | 合并改名（原"长文本与多模态"+"效率与稳定性"） | ✓ |

**权重原则**：推理 / 数学 / 代码占比偏大（对齐 OpenCompass）；安全与价值观单独打分不参与排名（对齐智源做法）。

**实施影响**（迭代 1 落地）：
- `executor.py:108-109`、`tasks.py:265/300`、`report_builder.py:603` 硬编码维度字符串改为引用 `DIMENSIONS` 常量
- 题库池重配：数学、安全两维需新出题（含生成式题型，安全题按出题指导原则编写）
- 历史评测记录不受影响（维度为自由字符串）

### 迭代 0：工程质量基线（横切，贯穿全程）

- CI 常态化回归测试：每个迭代验收强制「先回归测试 → 实现 → 全绿」
- 建立指标与任务类型的扩展点接口（metrics 注册表、任务类型注册表），避免后续迭代返工
- **立调度器接口契约**（队列/优先级/并发配额），供迭代 7 串行落地，避免与迭代 1~6 的 main.py 改造冲突
- 安全基线不变量持续生效：Key 不落盘、SSRF 防护、鉴权/审计/限流对新接口自动接入

### 迭代 1：任务模型与评测集地基
- `datasets.py`：task 新增 `type: 判别式|生成式`、`difficulty`、`tags`
  - 判别式：`expected` 必填（比对用）
  - 生成式：`expected` 可选、`rubric_note` 必填（评分依据）
- `parsers.py` 四格式解析器（JSON/CSV/MD/TXT）支持 type，缺省 = 判别式，向后兼容
- **八维体系落地**：`DIMENSIONS` 常量重定义、魔法字符串清理、题库池重配（数学/安全新增题）
- 评测集元信息（版本、来源）；前端上传/预览展示任务类型徽标
- **模型配置库**：本地保存 N 套模型配置（URL/名称/采样参数，Key 不落盘沿用安全基线），支持增删改查与页面选择，为 N 模型 benchmark 提供输入
- **验收**：混合题型评测集完整解析入库；老格式零破坏（回归红线）；新旧解析器测试全绿；模型配置库增删改查通过

### 迭代 2：指标引擎与统计体系

- 新增 `metrics.py`，按任务类型分支：
  - 判别式：Top-1 准确率、F1、精确匹配、**松弛准确率**（数值题容差）
  - 生成式：语义相似度（embedding provider 抽象）、rubric 结构化打分、一致性、**BLEU/ROUGE n-gram 指标**
  - **答案提取分层**：先规则/精确匹配 → 未命中再 LLM 解析（VLMEvalKit 模式）
- **提示模板体系（执行侧）**：executor 支持直答 / Zero-shot CoT（"让我们一步步思考"）/ Few-shot 示例注入
- **统计显著性**：胜率、bootstrap 置信区间、「差异不显著」提示
- **截断/无效作答处理**：truncated 与 API 错误转为评审提示 + 报告警告
- **预算熔断**：预估 token 成本，超限中止/提醒
- KPI（耗时、token）结构化采集；报告页按「判别式/生成式指标区」分区 + KPI 区
- **AI 评审接入**：`StartRequest` 新增 judge 评审配置字段，`judge.py` 接入 `main.py` 主流程（作答完成 → 自动评审 → 报告），评审配置/评审方优先级可在页面配置
- **验收**：全部指标为纯函数可测，mock 数据全绿；embedding provider 可切换，缺省离线可用；AI 评审接入后全链路（作答→评审→报告）mock 跑通

### 迭代 3：多评审方升级 + 评审可靠性工程

- `judge.py` 评审方抽象：第三方评审 Agent（独立于被测模型的评审配置）＋ 人工评审（复用双盲机制）
- 评审策略：纯 Agent / 纯人工 / 混合加权融合，评审方身份与权重可配置
- **评审模式扩展（N 模型）**：单臂参考评分（rubric + 金标锚定，无需配对，成本 O(N)）与全两两对决（成本 O(N²)，仅 top 候选）可配置；排行榜场景默认单臂
- 生成式任务 rubric 评审打分协议（结构化输出）
- **逐题独立随机交换 X/Y**：每题独立打乱 + 每题 reveal 映射表，消除位置偏差
- **金标校准集 + 元评估**：人工权威评分金标题 → 评审-人工相关性（Spearman）、评审内部一致性（Cohen's Kappa）
- **重点案例人工复核**：按评分/分歧度筛选 Top 案例显式进入人工复核
- **评审健康度与降级**：invalid 率阈值告警；评审模型故障时可配置切人工
- **验收**：三种策略切换跑通；元评估指标随报告展示；复评一致率可见

### 迭代 4：LLM 出题与内容质量

- 出题 pipeline：分类型/维度提示词模板 → 模型生成 → 自动校验（去重、可解性、rubric 完整性、防泄漏检查）→ 待审核状态入库
- **出题模板带 CoT 配置**（题面支持思维链引导与 Few-shot 示例）
- 人工审核页（编辑/通过/驳回）；评测集版本化（历史评测引用版本快照）
- **RAG/上下文忠实性评测**：文档+问题形态，测 Faithfulness / Answer Relevancy（grounding 评测：模型是否忠实于给定上下文、拒绝编造，不需要真实检索系统）
- **评测环境快照**：种子/温度/模型名/评审配置指纹随记录保存，报告页展示
- **可复现评测包导出**：题目+答卷+配置+种子一键导出，供外部复核
- **验收**：一键生成 → 审核 → 入库 → 用于真实评测全链路打通

### 迭代 5：Bad Case 体系

- 错误分类 taxonomy 细化五类：事实错误 / 语义错误 / 推理错误 / 安全偏见输出 / 执行错误
- **LLM 自动归因打标 + 人工校验**：失败案例输入 LLM 判断错误来源并打标签，人工抽验
- 自动挖掘：低分题、双模型均败题、评审分歧题、**边缘情境样本**入池
- 诊断与可视化：bad case 列表、分类分布、**错误类别分布图**、归因报告
- **反馈闭环**：bad case → 题库修订建议 → 回归重测
- **基准饱和度监测**：历史通过率上升趋势 → 提示题库过时需更新
- **验收**：一次评测后自动产出 bad case 库，可标注/可导出/可跳转回报告原文

### 迭代 6：对抗鲁棒性、偏见测试与高级可视化

- 对抗样本生成（改写 / 扰动 / 噪声注入，复用迭代 4 生成与校验能力）与鲁棒性衰减曲线（扰动强度 → 得分）
- **偏见与公平性测试**：同一题做性别/地域/文化属性扰动对照，测回答一致性、歧视性检测（复用同一扰动管线）
- 多折线图（横轴 K 值、纵轴召回率，每条线一个模型）、箱线图（得分分布）、散点图（得分-延迟/Token）
- KPI 看板：评测耗时、token 消耗趋势、**硬件利用率（CPU/GPU）**
- **N 模型排行榜报告形态**：分维度排名表 + 综合分 + 胜率矩阵 + bootstrap 置信区间标注（题数不足标「差异不显著」），替代双模型 winner/loser 结论段；雷达图支持多模型叠加
- **验收**：图表类型齐全，且有真实评测数据可演示

### 迭代 7：资源管理与任务调度

- 调度器：任务队列、优先级、并发配额，替代现有固定并发 2
- **benchmark 批次**：一个批次 = 1 任务集 × N 模型 × M 轮重复，每模型一个执行单元，复用队列/优先级/配额
- 资源抽象层：CPU 并发池化与弹性分配落地；GPU 仅预留接口（不实现）
- **硬件利用率采集接口**（CPU/GPU，供迭代 6 KPI 看板）
- **断点续跑**：瞬时结果增量落盘，API 故障后重试失败题续跑
- 前端：任务列表、排队状态、取消、优先级调整
- **验收**：多任务排队/抢占/取消演示通过；调度与续跑单元测试全绿

### 迭代 8：整体打磨

- 配置页 / 报告页 / 评审页布局统一优化，空态/加载态/错误态完善；评测环境快照展示
- 全量回归 + 性能基准（长文本、多任务并发）
- 用户手册与演示脚本
- **验收**：全量测试绿；三大页面无已知体验缺陷

