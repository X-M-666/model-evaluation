# 迭代七验收报告：资源管理与任务调度

> 基线 commit `7e73396`（迭代六，902/30）→ **迭代七 949 passed / 30 skipped（0 失败）**
> 测试命令：`cd webapp && python -m pytest tests -q --tb=short -p no:cacheprovider`
> 新增用例：12（scheduler）+ 20（resume）+ 9（tasks_api）+ 10（batch）+ 4（storage 批次/增量/queued）+ 1（xss_playwright tasks 页）≈ 47 全绿

## 一、验收结论

| 验收项 | 结论 | 证据 |
| --- | --- | --- |
| 全量回归零失败 | ✅ | 949 passed / 30 skipped（迭代六基线 902 + 新增 47） |
| 调度器：队列/优先级/并发配额（替代固定并发 2） | ✅ | `backend/scheduler.py` 纯逻辑（优先级排序 + FIFO + 配额 + 排队取消/改优先级），`test_scheduler.py` 12 用例 |
| 排队替代 429 | ✅ | `start_eval` 超并发自动入队（200 + queued SSE 事件 + 排队位置），`test_limits` 演进为排队断言 |
| 多任务排队/抢占（优先级插队）/取消演示 | ✅ | 排队视图 + 优先级调整 + 排队取消独立分支（出队+删目录+审计），`test_tasks_api.py` 9 用例 + tasks.html |
| benchmark 批次（1 任务集 × N 模型 × M 轮） | ✅ | `POST /api/benchmark`（模型库 id + Key 内存）+ N 个执行单元入队，`test_batch.py` N=5 全链路 |
| 调度与续跑单测全绿 | ✅ | `test_resume.py`（persist_cb 逐题增量落盘 / skip_ids / resume API 语义）+ `test_scheduler.py` |
| N=5 benchmark 批次跑通 | ✅ | test_batch N=5 全链路（mock 单臂）→ done + 排行榜聚合；部分失败 → partial + 失败模型 N/A |
| 硬件利用率采集接口 | ✅ | 迭代六 `hwmon` 已就绪（CPU 增量采样 + GPU 预留 N/A），本期直接复用 |
| 断点续跑 | ✅ 手动 | 执行期逐题增量落盘（`answers-inc.json` 按侧幂等合并）→ 磁盘态任务 `POST /api/eval/{id}/resume` 重入队，跳过双侧已完成题 |
| 任务管理页 | ✅ | `tasks.html`（排队/运行/优先级调整/取消/batch 创建与进度/排行榜跳转，CSP nonce 托管） |

## 二、功能验收明细

### 2.1 调度器（backend/scheduler.py，新增，纯逻辑零 asyncio）

- `submit`（-priority, enq_seq）排序、同优先级 FIFO；`next_batch` 配额内弹候选
  （高优先级插队 = 排序天然实现）；`release` 释放配额补派发（幂等）；
  `cancel_queued` / `set_priority`（仅排队中）；`queue_view`（含位置）。
- mock job 不进调度器（沿用「不消耗外部资源不计配额」语义）。
- 配额可弹性配置：`MODEL_DUEL_CONCURRENCY` 环境变量（默认 2），即 CPU 池化 v1 形态。

### 2.2 调度接线（main.py）

- **排队化**：`start_eval` 删除 429 门禁 → `_SCHEDULER.submit` → `_dispatch_pending`
  （配额内派发建 asyncio task）；仅真正排队时推 `queued` 事件（含位置）+ 审计
  `eval_queued`。tasks.json/env 快照**延后到派发时落盘**（排队中磁盘态 = queued）。
- **配额释放**：`_scheduler_release` 幂等，挂接 reviewing 入口（pure_human / hybrid /
  fail_open 降级）+ `_task_done` 兜底（completed/error/cancelled 全覆盖）。
- **排队取消**：`DELETE /api/history/{id}` 对 queued 任务走独立分支（出队 + 删目录 +
  审计，无 task.cancel）；运行中保持 cancelling 语义。
- **重启沉降**：lifespan 将磁盘态 queued 任务置 error（「排队任务随重启取消，
  v1 内存队列不自动恢复」），历史列表可见不悬置。
- `GET /api/tasks`（quota/queued+running 视图/batches）、`PUT /api/tasks/{id}/priority`
  （queued 可改重排序 + 审计，运行中 409）。

### 2.3 benchmark 批次（1 任务集 × N 模型 × M 轮，单臂评审）

- `POST /api/benchmark`：模型配置库 id（Key 取进程内存，未补录 400）、评审模型
  SSRF、预算按 N 模型放大（`budget.estimate_tokens(..., n_models)`，hard 400）；
  生成 batch_id + N 个执行单元同优先级入队；审计 `benchmark_started`。
- **执行单元 `_run_batch_job`**：M 轮单模型作答（复用 `_execute_model`）→ 判别式
  `score_task_metric` 逐轮均值 / 生成式 `run_single_arm_judge` 逐轮单臂均值
  （judge 缺省该类题 N/A + warning）→ 单臂 verdict + slim report → completed。
- **聚合 `_on_batch_job_done`**（幂等计数）：全部终态 → 复用迭代 6
  `build_leaderboard`（扩展单臂 verdict 抽取：无 revealed → 模型名取
  config.model_a.name + scores[].score）→ 排行榜落盘；部分失败 → batch partial +
  `failed_models` N/A 标注，排行榜排除失败模型；不足 2 个完成模型 → 无排行榜 + note。
- `GET /api/benchmark` / `{batch_id}`（进度聚合）/ `{batch_id}/leaderboard`；审计
  `benchmark_done`（partial 标志）。

### 2.4 断点续跑（手动 resume）

- executor `_execute_model`/`execute_all` 新增 `skip_ids`（跳过执行）与
  `persist_cb(label, task_id, entry)`（逐题回调，含失败/重试条目）。
- storage `answers-inc.json` 按侧键 `a:T1`/`b:T1` 幂等合并（双模型不互相覆盖）；
  `partial_answers_count` 按题去重。
- `POST /api/eval/{id}/resume`：仅磁盘态（`_jobs` 无条目）且 state∈{executing,error}
  且存在部分答案可续跑（运行中/无部分答案 409、不存在 404）；重入队 → 派发 →
  `_run_eval_impl` 恢复路径：skip_ids = 双侧均有答案的题，执行后按侧合并回答案池，
  persist_cb 继续增量落盘（可再次续跑）；审计 `eval_resumed`。

### 2.5 存储与兼容

- `_job_state` 补 `queued`（有 config.json 且无 tasks.json），原 fallback `pending`
  仅无 config 时保留（`test_storage` 锁定 + 新增 queued 分支用例）。
- `BATCHES_DIR = .eval/batches/` + BATCH_ID_RE + 批次 CRUD（防路径穿越正则校验）；
  `get_job_files` 白名单不含 answers-inc（内部运行时文件，不入导出包）。
- 既有双模型流程零改动语义：所有既有集成测试保持绿（949 全绿含
  test_agent_judge/hybrid/recovery/export 等 113 项评审链路用例）。

## 三、实现中的问题修复记录

| 问题 | 修复 |
| --- | --- |
| 已派发任务因 asyncio 时序未及时转 executing → 误推 queued 事件 | `job_id not in _SCHEDULER.running()` 判定，仅真正排队时推送 |
| 增量答案单键 {task_id} 双模型互相覆盖 | persist_cb 带 side 标签，键为 `a:T1`/`b:T1`，合并按侧补齐 |
| 批次测试模型库重名跨用例污染 | fixture 按用例清空 MODELS_DIR + 内存 Key |
| test_batch 审计断言读旧快照 | 聚合完成后重新 read_events |
| tasks.html 排队取消后调度器引用错位 | 测试临时调度器保持到断言结束（finally 恢复） |
| 既有 429 并发测试语义变化 | 演进为排队断言（200 + state queued + 位置） |

## 四、遗留与说明

- 运行中任务不可抢占（优先级仅排队中生效）；v1 内存队列，重启排队任务沉降 error
  （不自动恢复）——均为文档化行为。
- batch 生成式题单臂评审需 judge 配置，缺省该类题得分 N/A + warning（判别式
  纯指标免评审）；预算预估按 N 模型 × M 轮放大，hard 模式可拒绝。
- batch 执行单元暂不支持断点续跑（resume 面向常规评测任务）；批次进度前端轮询。
- 常规评测 resume 仅跳过「双侧均已完成」的题；单侧完成（崩溃于执行中途）的题
  重新执行（增量落盘幂等覆盖）。

## 五、建议后续（迭代八候选）

- 任务列表页 SSE 实时刷新（当前轮询 3s 兜底）+ 批次创建后自动跳转任务页。
- benchmark 批次支持「运行中暂停/整体取消」与批次重跑。
- 队列持久化（重启恢复排队）与运行中抢占（配额抢占策略）。
