# 迭代五验收报告：Bad Case 体系与基准饱和度

> 基线 commit `5b060c8`（迭代三收尾，708/30）→ 迭代四验收（781/30）→ **迭代五 816 passed / 30 skipped（0 失败）**
> 测试命令：`cd webapp && python -m pytest tests -q --tb=short -p no:cacheprovider`
> 新增用例：8（badcase 存储）+ 20（badcase 挖掘/归因）+ 7（badcase API/饱和度）= 35 全绿

## 一、验收结论

| 验收项 | 结论 | 证据 |
| --- | --- | --- |
| 全量回归零失败 | ✅ | 816 passed / 30 skipped（迭代四基线 781 + 新增 35） |
| 一次评测自动产出 bad case 库 | ✅ | `_finalize_job` 完成时同步挖掘入库（纯规则、零网络），`test_job_completion_mines_bad_cases_and_saturation` |
| 可标注（人工确认/改标/驳回） | ✅ | `POST /api/badcases/{id}/attribution` + 审计，`test_human_attribution_confirm_and_revert` |
| 可导出（含修订建议） | ✅ | `GET /api/badcases/export`（JSON 下载），`test_export_json` |
| 可跳转回报告原文 | ✅ | badcases.html 每条 case「查看报告原文 → report.html?job=<id>」 |
| LLM 自动归因 + 人工抽验 | ✅ | 归因跑批（线程 loop）+ 白名单解析 + 失败降级未归类，`test_llm_attribution_async_updates` / `..._failure_keeps_uncategorized` |
| 饱和度监测空态/有数据态 | ✅ | `saturation_trend` 纯函数 + 路由 trend 段，`test_saturation_trend.py`（7 用例） |
| 零真实网络 / 零密钥落盘 | ✅ | conftest 封锁 + 归因复用 SSRF 校验、redact_sensitive |

## 二、功能验收明细

### 2.1 错误分类 taxonomy 与挖掘（engine/badcase.py）

- 五类：事实错误 / 语义错误 / 推理错误 / 安全偏见输出 / 执行错误（`BAD_CASE_CATEGORIES`），未归类兜底。
- `mine_bad_cases` 纯函数（零网络、确定性可测）：
  - 低分（单侧 <3.0，model=x/y）
  - 双模型均败（max<4.0，model=both）
  - 评审分歧（|x−y|≥3.0）
  - 边缘情境：安全维低分（<6.0）/ RAG grounding 不通过 / 代码验真未全过（edge_safety / edge_grounding / edge_code）
  - 多规则命中合并去重（每题一条，sources 合并）；答案原文截断 2000 字入 evidence；评审 basis/winner 随证据保存。

### 2.2 LLM 自动归因（复用 review.judge，可覆盖）

- `attribute_badcase` 异步服务：prompt 含题面/rubric/context/双答案/得分/winner → 输出 `{category 五选一, basis, suggestion}`。
- 解析白名单校验（非法类别/非 JSON → None）；调用异常/无 judge 配置 → 保持未归类，绝不阻塞主流程。
- 触发：`_finalize_job` → `_spawn_attribution` 独立 daemon 线程（自带事件循环）跑批，逐条更新 + 审计 `badcase_attribution(path=llm)`。
- 人工抽验：归因页展示 LLM basis/suggestion，人工确认/改标/驳回归因（审计 path=confirm|revert）。

### 2.3 诊断可视化与反馈闭环（前端）

- 新页面 `badcases.html`（CSP nonce 托管）：job/分类/确认态筛选、错误类别分布图 + 挖掘来源分布图（ECharts 饼图）、归因报告（LLM 标记/人工确认徽标 + 可编辑确认）、导出按钮、跳转报告原文。
- `report.html`：Bad Case 入口（带计数）+ 概览段（总数/已确认/分类 top）。
- 反馈闭环：归因 suggestion（修订建议）随导出清单输出；前端引导至报告与出题页人工修订（v1 人工闭环，不自动改题库）。

### 2.4 基准饱和度监测

- `update_saturation` 增加可选 `dataset` 参数（job 级记录，旧签名兼容零破坏）；`_finalize_job` 完成时按 job_id 幂等写入逐题结果。
- `stats.saturation_trend` 纯函数：得分率=双侧评审分均值/10；按数据集分组、每 task 时间序前半/后半均值差 ≥ +15% 且两侧各 ≥3 次 → 「题库可能过时」；空态 `available=False` 提示待数据积累。
- `GET /api/stats/saturation` 响应新增 `trend` 段（`jobs` 旧字段兼容，`test_models_registry::test_api_stats_saturation_empty` 同步演进）。
- `index.html` 评测集区饱和度提示条（仅饱和数据集出现）。

### 2.5 存储与 API

- `.eval/badcases/<case_id>.json`：case_id = `bc_<job_id>_<task_id消毒>`（正则校验防路径穿越）；CRUD + `update_badcase_attribution` + `export_badcases_json`。
- API：`GET /api/badcases`（筛选）、`GET /api/badcases/{case_id}`、`GET /api/badcases/stats`、`POST /api/badcases/{case_id}/attribution`（400/404 语义）、`GET /api/badcases/export`、`GET /api/stats/saturation`（trend）。
- 路由顺序约束：`/stats`、`/export` 须定义在 `/{case_id}` 之前（已实现，测试覆盖）。
- 审计事件：`badcase_mined`、`badcase_attribution`（path=llm|confirm|revert）。

## 三、实现中的问题修复记录

| 问题 | 修复 |
| --- | --- |
| `/api/badcases/stats`、`/export` 被 `/{case_id}` 路由抢占 → 400 | FastAPI 路由顺序调整：静态路径定义在参数路径之前 |
| 归因协程从未被调度（asyncio.run 收尾即关闭 loop，sleep(0) 无效） | `_spawn_attribution` 独立 daemon 线程 + 自带 asyncio.run 事件循环，测试与真实环境均确定运行 |
| list_badcases 分类显示滞后（顶层 category 未随归因更新） | 摘要 `category` 改为 `attribution.label` 优先（LLM/人工归因后即时生效） |
| 挖掘测试数据与阈值冲突（2.0 vs 8.0 分差 6 同时命中分歧） | 调整测试分数组合使信号单一（2.0 vs 4.5 等） |
| 既有 `test_api_stats_saturation_empty` 严格相等断言被 trend 段破坏 | 演进断言：jobs 空 + trend 空态（兼容旧客户端字段） |
| 饱和度趋势首版实现含冗余循环 | 重写为按数据集分组 + 每 task 时间序聚合的干净实现 |

## 四、遗留与说明

- 归因 LLM 复用 `review.judge`（迭代四决策），无独立归因配置字段；v1 不引入新 schema。
- 反馈闭环 v1 为「修订建议导出 + 人工修订引导」，不自动改题库（与方案一致）。
- `.eval` 运行时数据目录位于仓库上一级（`D:\open\.eval`，gitignore 外），测试全程重定向临时目录，不污染。
- 饱和度趋势依赖历史评测积累（每次 completed 写入 saturation.json），空态提示已就绪。
- 归因跑批在 daemon 线程执行：审计/存储为线程安全（audit 加锁、case 文件独立），uvicorn 多请求下无共享写竞争。

## 五、建议后续（迭代六候选）

- 对抗扰动管线（改写/噪声/属性扰动）与鲁棒性衰减曲线（复用出题校验链）。
- N 模型排行榜报告形态（分维度排名 + 胜率矩阵 + bootstrap CI 标注）。
- bad case 修订建议 → 一键生成修订版评测集草稿（半自动闭环）。
