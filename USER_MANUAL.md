# 模型对决评测平台 · 用户手册

> 面向评测使用者：从启动到读懂报告的完整操作指南。版本对应迭代八（整体打磨）交付。

## 1. 平台能做什么

输入模型 API 配置（OpenAI 兼容），自动完成 **出题 → 作答 → 评审 → 报告** 的评测闭环：

- **双模型对决**：一次评测两个模型，人工双盲 / AI / Hybrid 评审，输出对比报告
- **N 模型横向 benchmark**：一次创建批次，多个模型跑同一评测集，产出排行榜
- **对抗扰动与偏见测试**：改写 / 噪声 / 属性扰动下测鲁棒性与公平性
- **LLM 出题**：AI 生成题目 → 人工审核 → 入库评测集
- **饱和度监测**：评测集题目饱和提示（跨 job 汇总）

八维能力体系：知识 / 数学 / 逻辑推理 / 代码 / 语言 / 指令遵循与对齐 / **安全与价值观**（单独展示不计入总分）/ 长文本与效率稳定性。

## 2. 快速启动

```powershell
cd webapp
pip install -r requirements.txt
.\run.ps1                # 默认监听 127.0.0.1:8910
```

访问 `http://localhost:8910`。局域网共享部署见 README「部署与安全」（需设置 `MODEL_DUEL_TOKEN`）。

## 3. 四个评测入口

### 3.1 N 模型比较（任务页 → 新建任务）

1. 进入任务页点「**新建任务**」，4 步向导：① 模型选择 ② 评测工具 ③ 数据集 ④ 其他配置
2. ① 填写 **2~20 个模型**的 API URL、Key、模型名称（「+ 添加模型」增行；Key 仅存内存，不落盘；temperature 0.7 / max_tokens 16384 固定默认），可点「**验证连通性**」逐模型测试
3. ② 评测类型：判别式自动计分 / 生成式 AI 评审（选后者填评审模型）；代码验真模式；重复次数（>1 取平均 + 稳定性）
4. ③ 数据集：内置题库（勾选维度 + 题目数量）/ 自定义评测集（上传 / 粘贴 JSON / 模板下载 / 已有评测集管理）
5. ④ 优先级、随机种子、任务名称 → 「创建任务」
6. 提交后任务表格实时刷新（SSE），完成后排行榜页查看分维度排名与胜率矩阵

### 3.2 批次任务监控（任务调度页）

1. 排队 / 运行视图实时刷新（SSE），可调整优先级（仅排队中）与取消任务
2. **评测任务表格**：任务名称 / 评测模型 / 评测工具 / 状态 / 综合得分 / 数据集，支持关键词搜索与状态筛选
3. **LLM 出题**：同页配置出题模型（留空复用向导评审模型；URL 填 /v1 自动拼接端点）→ **维度可多选**（每个选中维度生成所选数量道题）→ 生成批次（实时进度）→ 审核页批准/驳回/编辑 → 入库（版本化）→ 完成后可删除批次（已入库题目不受影响）
4. **访问令牌**：页面顶部设置（共享部署模式；单机无需填写）

### 3.3 文本对战（文本对战页）

1. 填写**模型 A / 模型 B** 的 API URL、Key、名称；可选「**盲评模式**」（隐藏身份：侧栏只显示「模型 A / 模型 B」代号，且每道题两个真实模型**左右位置随机打乱**，避免主观偏好）
2. 设置测试题总数（1~50）与测试集来源（随机内置题库 / 自定义评测集），点「开始对战」——首题立即让两个 AI 并排流式作答
3. 双模型**同题并排流式作答**（可点击输出区停止）；每题结束后 5 档人工投票：A更好 / A略好 / 平手 / B略好 / B更好——**投票后停留本题**，点「下一题」才让 AI 作答下一道（「上一题」回看已答内容，不重复消耗 API）；盲评模式下**全部题评完**后才可在「结果统计」弹窗点「**揭晓模型身份**」查看左/右栏对应的真实模型
4. 「结果统计」弹窗查看总体统计（总题数 / 已评分 / 未评分 / A 胜 / B 胜 / 平局）与逐题详细评分
5. 「重新对战」重新抽题；「**退出对决**」结束当前对局回到初始设置（投票与作答丢弃，模型配置保留）；**跳转/刷新页面不重置对局**（会话级持久化到 sessionStorage，关浏览器自动清除）；Key 不落盘

### 3.4 扰动鲁棒性（集成于评测流程）

不再需要单独页面：新建任务向导第 2 步开启「**扰动评测**」并选择模式（改写 / 噪声注入 / 属性扰动·性别·地域·文化，默认改写+噪声注入）后：

1. 评测完成自动对**每个被测模型**跑扰动版题目（同一评测集；代码题与安全维题目自动跳过）
2. 排行榜页「**鲁棒性评测**」区块展示：每模式衰减曲线（扰动强度 → 平均得分，各模型一条线）+ 偏见与公平性对照表（原版 vs 扰动分差、一致性、疑似歧视标注）
3. 扰动与主评测并行执行；批次完成判定包含扰动任务全部结束

> 属性扰动无槽位命中时该模式无对照数据（提示说明）；生成式题需配置评审模型才有评分。

## 4. 模型配置（纯内联）

- 首页为入口页（「进入评测工作台」跳转任务页）；模型在**任务页新建任务向导**第 1 步行内填写 URL / Key / 模型名称，无独立配置库
- **API Key 仅存进程内存**：走请求体提交，不落盘、不入配置库，重启即失效，每次评测需重新填写
- 默认参数固定：temperature 0.7 / max_tokens 16384 / top_p 不设置；「验证连通性」逐模型校验 URL 可达性与模型名有效性

## 5. 评测集管理

| 途径 | 说明 |
|---|---|
| 内置题库 | 八维题库池随机抽题，固定种子可复现 |
| 自定义上传 | JSON / CSV / Markdown / TXT 四格式（页面有模板），支持判别式/生成式、context、tags |
| LLM 出题 | 任务页配置出题模型 → 生成批次 → 审核页批准/驳回/编辑 → 入库（版本化） |
| rag_demo | 内置 RAG 演示集（生成式 + 参考文档，演示上下文忠实性评测） |
| longtext_bench | 内置长文本基准（2K/8K/32K 材料内嵌细节提取，性能测试用） |

## 6. 评审方式

| 方式 | 流程 |
|---|---|
| 人工双盲 | 作答完成后进入评审页，每题对 X/Y 各打 0-10 分（身份随机隐藏），提交后揭示并生成报告 |
| AI 自动评审 | 评审模型逐题打分（可配置失败降级人工） |
| Hybrid | AI 预评全部题目，仅 Top-K 分歧/低分题转人工复核 |

评审质量可度量：金标校准集 → Spearman / Kappa 元评估段随报告展示。

## 7. 报告解读

- **核心指标**：总分 / 平均分 / 胜场分布 / 统计显著性（bootstrap CI，「差异不显著」标注）
- **图表区**：逐题得分、维度雷达、延迟、Token 构成、代码通过率、散点
- **精确分析**：规则化统计结论
- **环境快照**：OS / Python / 依赖版本 + 评测参数（seed/温度/模型/评审指纹/数据集版本）——复现依据
- **导出**：评测包（zip 含题目/答卷/配置/环境快照）与 JSON

## 8. 任务调度

- 超并发评测自动**排队**（页面可见位置），可调整优先级（仅排队中）与取消
- **断点续跑**：执行中断后，历史列表对磁盘态任务点「恢复」重新入队续跑（跳过已完成题）
- **批次控制**：评测任务表格对运行中批次显示「取消」，对已完成批次显示「删除」（删除联动清理该批次排行榜与全部评测数据，不可恢复）；已完成批次如需重跑，在任务页新建任务重新填写模型（Key 不落盘，无法自动复用）
- **任务表格**：综合得分取自排行榜聚合（完成批次显示，未完成显示 `-`）；搜索匹配任务名称/模型/数据集，状态筛选含全部/运行中/排队中/已完成/部分失败/失败/已取消
- 重启后排队任务不自动恢复（内存队列，历史中显示为 error 提示）

## 9. 排行榜与 KPI 概览

- **排行榜**：选择多个已完成评测（同一评测集）聚合——分维度排名、综合分、胜率矩阵 + CI 标注、多模型雷达、箱线图、散点、K-召回率；**评测批次完成后自动生成**（含扰动评测结果：衰减曲线与偏见对照）
- **KPI 概览**（迭代十一：原 KPI 看板融入排行榜页）：已完成评测数 / 累计耗时 / 累计 Token / 评测耗时趋势 / Token 消耗趋势 / 评测明细（job/模型/耗时/Token/创建时间）；硬件利用率与 Bad Case 功能已移除

## 10. 一键演示

```powershell
cd webapp
python -m scripts.demo_seed        # 生成 3 条报告 + 排行榜 + 扰动记录（写入正式目录）
python -m scripts.demo_seed --tmp  # 写入临时目录，不污染数据
```

生成后按脚本打印的路线图逐个页面查看。本地性能基准：

```powershell
python -m scripts.benchmark_local --tasks 20 --rounds 3   # mock 管线吞吐（零网络）
```

## 11. 常见问题

| 问题 | 说明 |
|---|---|
| 模型成绩全 N/A / 空 | 模型名称填错或 API 不可达：先点「验证连通性」确认每行通过 |
| 生成式题得分 N/A | 未配置评审模型（单臂评分需要）；判别式题不受影响 |
| 「差异不显著」标注 | 题数不足或 CI 跨 0，bootstrap 判定，属正常统计口径 |
| 排队任务消失 | v1 内存队列，重启不恢复（历史列表有 error 提示） |
| 代码验真不可用 | 需部署方配置 `MODEL_DUEL_SANDBOX_PYTHON` 并运行自检（见 README） |
| 长文本题很贵 | longtext_bench 是显式选择的评测集，普通内置抽题不含长文本题 |

## 12. 已知限制

- 纯文本评测（非目标：多模态 / 多轮 Agent）
- 优先级仅对排队中生效（运行中不抢占）
- batch 执行单元不支持断点续跑（resume 面向常规评测）
- 队列不持久化（重启排队任务沉降 error）
