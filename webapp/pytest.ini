[pytest]
markers =
    native: Windows 专属隔离后端测试（AppContainer / SID / Job Object）；非 Windows 平台由 CI 以 -m "not native" 排除
    perf: 性能基准（长文本边界/并发压力/大批次），默认排除于回归（CI 以 -m "not perf" 排除；验收时单独 pytest -m perf）
