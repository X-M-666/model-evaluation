# 模型评测平台 · 迭代六实施方案（完整定稿）

> 对应《迭代计划》迭代 6（对抗鲁棒性、偏见测试与高级可视化）、《需求迭代文档》§5.6、
> 《技术设计文档》§5.5/§6.5/§7。基线：迭代五完成态（816 passed, 30 skipped）。
> 本文记录范围决策（含用户已拍板决策）、模块设计、API 契约、测试计划与实施顺序。

## 一、范围与决策确认

### 1.1 范围总表

| 事项 | 落地 | 说明 |
|---|---|---|
| 对抗样本扰动管线 `perturb.py` | ✅ | 改写 / 噪声注入 / 属性扰动（性别/地域/文化），纯规则确定性可测；强度梯度参数化 |
| 鲁棒性衰减曲线 | ✅ | 强度 0（原版）→ 各强度扰动版得分折线，每模式一条 |
| 偏见与公平性测试 | ✅ | 属性扰动对照：逐题分差 + 回答一致性（embedding/离线 n-gram）+ 歧视性标记（阈值 1.0） |
| 扰动题安全过滤 | ✅ | 复用 generator 校验能力（`check_safety_static`） |
| 多折线图（K-召回率） | ✅ | 每模型按题得分降序，recall@K = 前 K 题中得分≥阈值(6.0)题数 / 达标题总数 |
| 箱线图（得分分布）/ 散点图（得分-延迟/Token） | ✅ | 排行榜视图内，按模型分组 |
| KPI 看板 | ✅ | 耗时/token 趋势（历史 job 聚合）+ CPU 利用率采样；GPU 无硬件 N/A |
| N 模型排行榜报告形态 | ✅ | 分维度排名表 + 综合分 + 胜率矩阵 + bootstrap CI「差异不显著」标注 + 多模型雷达 |
| 调度器/batch（迭代 7） | ❌ | 不依赖；排行榜聚合核心函数留好迭代 7 复用点 |
| 双模型 job 状态机 / 评审主流程 | ❌ | 零改动（扰动评测为独立异步任务） |

### 1.2 用户已拍板决策

1. **排行榜数据来源 = 历史 job 聚合**：`POST /api/leaderboard` 指定 N 个已完成 job
   （同一评测集），按 reveal 归一化抽取每模型每题得分；报告标注「双盲成对评审」口径；
   聚合核心函数可在迭代 7 被 batch 直接复用。
2. **K-召回率 = Top-K 达标覆盖**：每模型按题得分降序，recall@K = 前 K 题中
   得分≥阈值（默认 6.0）的题数 / 全部达标题数；阈值可配。
3. **扰动评测评审 = 单臂 rubric**：判别式题用指标命中率折算 0-10 分；生成式题复用
   `run_single_arm_judge`（需 judge 配置，缺省该类题仅输出文本指标与一致性、得分 N/A）。

### 1.3 关键设计定案

- **扰动评测为独立异步任务**（非 job 状态机）：仿出题批次模式（`gen_<id>` 独立协程槽），
  不动 `MAX_ACTIVE_JOBS` 与评审主流程；状态轮询，不扩 SSE。
- **扰动版任务 id 后缀 `-pN`**（原 id 保留在 `meta.origin_id`），prompt 替换、
  rubric/expected/context 保留——评审与指标口径与原版完全一致。
- **衰减曲线统一 0-10 分口径**：判别式 = 命中率折算（top1=10、relaxed 按比例×10、
  代码题按 code_verify 通过率×10）；生成式 = 单臂 rubric 分。
- **属性扰动 v1 纯规则槽位表**：性别（他/她、丈夫/妻子等）、地域（北京/上海/广州等）、
  文化（传统节日/饮食等名词）；无槽位命中 → 该题跳过并记 `no_slot`（报告可见），不阻塞整体。
- **CPU 利用率零新依赖**：Windows `ctypes.GetSystemTimes` / Linux `/proc/stat` 增量采样
  （系统级，口径注明）；macOS 及未知平台 → None（N/A）；核心换算 `_util_from_deltas` 纯函数可测。
- **评测耗时趋势**：`_finalize_job` 落盘 report 时补 `report["kpi"]["duration_sec"]`
  （新增字段，历史记录 N/A 空态，向后兼容）。
- **排行榜口径说明**：双盲对决评审分相对成对可比，跨 job 聚合为 v1 事实口径，
  `note` 字段明示；迭代 7 batch + 单臂接线后替换数据源，聚合逻辑不变。

## 二、模块设计

### 2.1 `backend/engine/perturb.py`（新增，纯函数为主）

```python
PERTURB_MODES = ["改写", "噪声注入", "属性扰动-性别", "属性扰动-地域", "属性扰动-文化"]
DEFAULT_INTENSITIES = {"改写": [1.0], "噪声注入": [0.1, 0.2, 0.3], "属性扰动-*": [1.0]}
BIAS_DIFF_THRESHOLD = 1.0

def perturb_prompt(prompt, mode, intensity, seed=None) -> str | None
    # 改写：内置同义词表按强度比例替换（确定性）；噪声注入：按强度插入干扰词/删除/错序；
    # 属性扰动：槽位规则表替换，未命中返回 None
def perturb_task(task, mode, intensity, seed) -> dict | None      # id=f"{tid}-p{seq}"，meta 记录 origin/mode/intensity
def build_perturb_set(task_set, modes, intensities, seed) -> dict  # 原版 + 扰动版平铺，跳过 None
def check_perturb_safety(task) -> bool                             # 复用 generator.check_safety_static
def score_task_metric(task, answer_entry) -> float | None          # 判别式 0-10 折算；代码题 code_verify
def build_robustness_curves(results) -> dict                       # {mode: [{intensity, score}]}，强度 0=原版
def bias_analysis(original, perturbed, embedder=None) -> list      # 每题 {score_diff, consistency, discriminates}
```

- 扰动题同过安全词表过滤（`check_safety_static` 拦截 → 该扰动版丢弃并记 warning）。
- 一致性相似度复用 `embed.py`（外部/本地/离线 n-gram 兜底），与迭代二采集语义向量同一机制。

### 2.2 `backend/hwmon.py`（新增）

```python
class CpuSampler:
    def sample(self) -> float | None   # 增量系统 CPU 利用率 0-100；不支持平台 → None
def _util_from_deltas(idle_delta, total_delta) -> float            # 纯函数
def collect_gpu_util() -> dict | None                              # 预留接口：v1 恒 None（N/A）
def collect_hw() -> dict                                           # {"cpu": {...}, "gpu": None, "generated_at"}
```

### 2.3 `backend/engine/leaderboard.py`（新增，聚合器）

```python
def extract_model_scores(job_id) -> dict | None
    # config.json + verdict.json + reveal.json：{model_name: {task_id: score, latency_ms, tokens}}
def build_leaderboard(job_ids: list[str]) -> dict
    # 校验：全部 completed；各 job tasks id 集合一致（否则 400 列出差异）
    # 产出：per_dim 排名 / composite / win_matrix + paired bootstrap CI（确定性 seed，MIN_SAMPLE=8）
    #       / k_recall（threshold=6.0）/ score_dist / scatter / radar / excluded_dimensions / note 口径
def compute_win_matrix(model_scores) -> dict
def compute_pair_ci(s1, s2, seed) -> dict      # {mean, ci_low, ci_high, n, significant}
def compute_k_recall(scores, threshold=6.0) -> dict
```

- bootstrap 复用 `stats.paired_bootstrap_ci`（题级配对 delta）；题数 < 8 或 CI 含 0 →
  `significant=False`，视图标「差异不显著」。

### 2.4 `schemas.py`（新增请求模型）

```python
class PerturbRequest(BaseModel):
    model: ModelConfig
    dataset_name: str
    modes: list[str]                      # ⊆ PERTURB_MODES，校验非法值 400
    intensities: dict[str, list[float]] | None = None   # None=各模式默认
    seed: int | None = None
    judge: ModelConfig | None = None      # 生成式题单臂评审；缺省该类题 score=N/A
    prompt_strategy: str = "cot"

class LeaderboardRequest(BaseModel):
    name: str | None = None
    job_ids: list[str] = Field(..., min_length=1, max_length=20)
```

### 2.5 `storage.py`（扩展）

- `PERTURB_DIR = .eval/perturb/`（运行产物，同 badcases 约定）；
  `LEADERBOARD_DIR = webapp/data/leaderboards/`（用户数据，同 datasets 约定）
- `save_perturb/load_perturb/list_perturb`、`save_leaderboard/load_leaderboard/list_leaderboards`
  （id 沿用正则校验模式，防路径穿越）
- 扰动结果结构：`{"perturb_id", "state", "model", "dataset", "modes",
  "per_task":[{origin_id, mode, intensity, score, consistency?, discriminates?, raw_answer}],
  "curves", "bias", "warnings", "progress", "created_at"}`

### 2.6 `main.py`（新增 3 组路由，不动既有流程）

- `POST /api/perturb`：SSRF 校验 model/judge URL → 槽位上限 3（独立于 generation，
  共享 `_GENERATION_MAX_ACTIVE` 模式）→ 后台协程：build_perturb_set →
  `executor._execute_model`（单模型，stability_repeat=None，embedding_cfg 传参）→
  判别式 `score_task_metric` / 生成式 `run_single_arm_judge`（judge 缺省 score=None）→
  组装落盘；进程中断遗留批次沉降 partial（仿 `_settle_generation`）
- `GET /api/perturb` / `GET /api/perturb/{perturb_id}`（轮询，含 progress）
- `POST /api/leaderboard`：校验 job_ids（completed + 评测集一致）→ `build_leaderboard` → 落盘；
  `GET /api/leaderboard` / `GET /api/leaderboard/{lb_id}`
- `GET /api/dashboard`：`hwmon.collect_hw()` + history jobs 聚合 `jobs_trend`
  （created_at/model/duration_sec/total_tokens，duration 取 `report["kpi"]["duration_sec"]`，缺省 N/A）
- `_finalize_job`：落盘 report 前附加 `report["kpi"]["duration_sec"] = end - start`

### 2.7 `audit.py`（新增事件）

- `perturb_started(perturb_id)`、`perturb_completed(perturb_id, count)`、`leaderboard_created(lb_id)`

### 2.8 前端（3 个新页面 + 入口，CSP nonce 托管沿用）

- **`perturb.html`**：配置（模型库选择/手填、评测集、模式多选 + 强度、judge 配置）→ 运行
  （轮询进度）→ 结果：鲁棒性衰减曲线（ECharts 多折线，强度→得分）、偏见对照表
  （原版 vs 扰动版逐题分差 + 一致性 + 歧视性徽标）、no_slot/safety 拦截 warnings
- **`leaderboard.html`**：历史 job 多选（前端提示需同一评测集）→ 创建；视图：综合分排名表、
  分维度排名表（Top 高亮）、胜率矩阵（表格 + CI 标注「差异不显著」）、多模型雷达叠加、
  箱线图（得分分布）、散点图（得分-延迟/Token）、K-召回率多折线
- **`dashboard.html`**：KPI 看板（总评测数/总耗时/总 token/CPU 当前利用率、GPU N/A 提示）
  + 耗时趋势 + Token 趋势折线（按时间）
- **`index.html`**：导航区新增 3 个入口链接

## 三、API 契约变更

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/perturb` | 创建扰动评测（SSRF 校验；非法 modes 400；judge 缺省生成式 score=N/A） |
| GET | `/api/perturb` / `/api/perturb/{id}` | 列表 / 详情（轮询 progress；404 语义） |
| POST | `/api/leaderboard` | 由 job_ids 聚合排行榜（非 completed / 评测集不一致 400） |
| GET | `/api/leaderboard` / `/api/leaderboard/{id}` | 列表 / 详情（含排名/胜率矩阵/CI/k_recall/radar 数据） |
| GET | `/api/dashboard` | KPI 看板（hw + jobs_trend） |
| GET | `/api/eval/{id}/report` | 新增 `kpi.duration_sec`（历史记录 N/A 空态） |

## 四、测试计划（先回归 → 实现 → 全绿）

### 4.1 前置（第 0 步）

1. 提交迭代五工作区全部改动（如未提交）；2. 全量回归基线 816/30 全绿后固化。

### 4.2 新增测试文件

| 文件 | 覆盖 |
|---|---|
| `test_perturb.py` | 三种扰动模式（改写替换比例/噪声强度梯度/属性扰动三组槽位命中与 None）、seed 确定性、id 后缀、安全过滤拦截样例、build_perturb_set 平铺结构 |
| `test_perturb_api.py` | POST 全链路（mock 上游：判别式 metric 得分、生成式 mock 单臂得分）、judge 缺省 N/A、轮询状态机（running→ready/partial/error）、审计事件 |
| `test_robustness.py` | build_robustness_curves（强度 0=原版、数据形态） |
| `test_bias.py` | bias_analysis（分差、一致性相似度、歧视阈值判定、embedder 注入与缺省 n-gram 兜底） |
| `test_leaderboard.py` | extract_model_scores（reveal 归一化 a/b 两侧）、综合分/分维度排名、胜率矩阵、paired bootstrap CI（显著与「差异不显著」）、k_recall、radar、排除维不计分、非 completed 与评测集不一致 400 |
| `test_dashboard.py` | kpi.duration_sec 落盘、jobs_trend 聚合、`_util_from_deltas` 纯函数、CpuSampler 注入 fake 平台（None 分支）、GPU N/A |

### 4.3 修改既有

- `conftest.py`：`_isolate_storage` 增加 PERTURB_DIR / LEADERBOARD_DIR 重定向
- `test_audit.py`（新事件）、`test_report_metrics.py`（kpi 新字段兼容）

### 4.4 验收映射

1. 图表类型齐全且真实评测数据可演示 → perturb.html（衰减曲线/偏见对照）+
   leaderboard.html（排名表/胜率矩阵/雷达/箱线/散点/K-召回率）+ dashboard.html（趋势/CPU），
   mock 数据即可演示
2. 3+ 模型排行榜演示数据可用 → 3 个 mock job → `POST /api/leaderboard` 全链路测试
3. 对抗样本生成（改写/扰动/噪声注入，复用迭代 4 生成与校验能力）→ test_perturb + 安全过滤用例

## 五、实施顺序

1. 第 0 步：落盘本方案 + 全量回归基线
2. `perturb.py` 纯函数层 + 单测（test_perturb / test_robustness / test_bias）
3. `hwmon.py` + `_finalize_job` duration_sec + 单测（test_dashboard）
4. schemas / storage / audit / conftest 扩展
5. main.py 扰动管线 + API + 集成测试（test_perturb_api）
6. `leaderboard.py` 聚合 + API + 集成测试（test_leaderboard）
7. 前端 perturb.html / leaderboard.html / dashboard.html / index 入口
8. 全量回归 + 验收三要素汇报

## 六、风险与缓解

| 风险 | 缓解 |
|---|---|
| 双盲对决分跨 job 聚合口径争议 | `note` 口径标注 + 迭代 7 batch 单臂接线后换源复用同一聚合 |
| 扰动评测成本（模型×题×模式×强度×评审） | 默认 2~3 档强度、判别式免评审、judge 可缺省、预算预估提示 |
| 属性扰动槽位命中率低 | 规则表 + `no_slot` 跳过并报告，不阻塞；文档说明可扩展槽位表 |
| CPU 采样跨平台不可用 | 纯函数核心 + 平台探测降级 N/A（GPU 恒 N/A 预留接口） |
| 历史 job 无 duration_sec | dashboard 空态 N/A，新评测起有值 |
| 新目录污染仓库 | conftest 重定向 + gitignore 覆盖 `.eval/` 与 `data/` 运行产物 |
| 扰动评审 invalid 率高 | 复用 `health_check` 阈值告警 + invalid 题 score 置 N/A 并记 warning |
