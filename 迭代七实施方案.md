# 模型评测平台 · 迭代七实施方案（完整定稿）

> 对应《迭代计划》迭代 7（资源管理与任务调度）、《需求迭代文档》§5.7、
> 《技术设计文档》§4.3/§4.4/§5.1/§5.3/§6.3/§7/§8。基线：迭代六完成态
> （902 passed, 30 skipped）。本文记录范围决策（含用户已拍板决策）、
> 模块设计、API 契约、测试计划与实施顺序。

## 一、范围与决策确认

### 1.1 范围总表

| 事项 | 落地 | 说明 |
|---|---|---|
| 调度器 `scheduler.py` | ✅ | 优先级队列 + 并发配额 + 排队取消/优先级调整（迭代 0 契约落地，替代 `MAX_ACTIVE_JOBS=2` + 429） |
| 排队替代 429 | ✅ | 常规评测超并发自动入队（200 + queued 事件），高优先级插队 |
| benchmark 批次 | ✅ | 1 任务集 × N 模型 × M 轮，每模型一个执行单元（单臂评审），复用队列/配额 |
| batch 评分 | ✅ 单臂 | 判别式 = `score_task_metric` 指标分；生成式 = `run_single_arm_judge` 单臂 rubric（judge 缺省该类题 N/A） |
| batch 排行榜 | ✅ | 全部终态聚合复用 `build_leaderboard`（扩展支持单臂 verdict）；失败模型标注 N/A 排除 |
| 断点续跑 | ✅ 手动 | 执行期逐题增量落盘 + `POST /api/eval/{id}/resume` 重入队续跑（跳过已完成题） |
| CPU 并发池化 | ✅ | 调度器并发配额即 CPU 池（env `MODEL_DUEL_CONCURRENCY` 可配弹性分配）；GPU 预留接口迭代 6 `hwmon` 已就绪，直接复用 |
| 任务管理页 | ✅ | `tasks.html`：排队/运行视图、取消、优先级调整、批次创建与进度 |
| 运行中抢占 | ❌ | 优先级仅对排队中任务生效（运行中不抢占，文档化） |
| 队列持久化 | ❌ | v1 内存队列，重启排队任务沉降 error（提示不自动恢复） |

### 1.2 用户已拍板决策

1. **batch 单臂评审**：判别式题指标得分、生成式题单臂 rubric（需 judge 配置）；
   成本 O(N×M)，与「N 模型默认单臂」基线一致。
2. **排队替代 429**：`MAX_ACTIVE_JOBS` + 429 逻辑替换为调度器配额（既有 429 测试演进为排队断言）。
3. **断点续跑手动触发**：执行期逐题增量落盘，用户对磁盘态任务显式 resume（防自动重跑意外计费）。
4. **batch 模型来源 = 模型配置库 id**：Key 取进程内存（未补录 400 提示），Key 不落盘不变量不变。

### 1.3 关键设计定案

- **调度器为纯逻辑模块（零 asyncio）**：`scheduler.py` 只管理「队列排序 / 配额 /
  派发候选 / 排队取消 / 改优先级 / 视图」，main.py 提供 `_dispatch_pending()` 胶水
  （弹候选 → 建 asyncio task）→ 单测无需事件循环。
- **排队期语义（设计文档 §4.3）**：submit 即建 `sse_queue`（排队中可连 SSE 收
  `queued` 事件）；排队中取消走独立分支（出队 + 删目录 + 审计，无 task.cancel）；
  `_job_state` 补 `queued`（有 config.json 且无 tasks.json）。
- **tasks.json 延后落盘**：submit 时仅 save_config（排队中磁盘态 = queued），
  dispatch 时 save_task_set + env 快照——与状态推断一致。
- **配额释放**：执行+评审阶段占配额；进入 reviewing / 终态 / 取消即释放
  （幂等 `release`，`_task_done` 兜底覆盖全部出口）。
- **batch job 复用单模型流**：`_execute_model`（迭代 6 已有）+
  `run_single_arm_judge`；verdict 为单臂格式 `{"scores":[{id,score,basis,_invalid}],
  "meta":{...rounds}}`；落盘 slim report.json 以满足 `completed` 磁盘态判定。
- **排行榜单臂兼容**：`extract_job_model_scores` 分支——无 `revealed` 时模型名取
  `config.model_a.name`，得分取 `scores[].score`，延迟/Token 从 answers 聚合；
  双盲旧格式不变。
- **部分失败聚合**：batch 聚合仅对 completed job 调 `build_leaderboard`
  （不足 2 个 completed → 无排行榜 + note）；失败模型在 batch 详情标注 N/A。
- **断点续跑**：executor 新增 `persist_cb(task_id, entry)`（每题落盘
  `<job>/answers-inc.json` 幂等合并）与 `skip_ids`（跳过题不执行）；resume 时
  `load_answers_inc` → 成功题集合 → skip + merge。仅磁盘态任务（`_jobs` 无条目、
  state∈{executing,error}、有部分答案）可 resume。
- **重启沉降**：lifespan 扫描磁盘态 queued job → 写 error.json「排队任务随重启
  取消（v1 内存队列，不自动恢复）」（磁盘无 cancelled 态，error 可见性等价）。
- **预算放大**：`estimate_tokens(..., n_models=1)` 兼容扩展，batch 预估 = 单 job × N
  （hard 超限 400 拒绝）。

## 二、模块设计

### 2.1 `backend/scheduler.py`（新增，纯逻辑）

```python
class Scheduler:
    def __init__(self, concurrency: int = 2): ...        # 配额（env MODEL_DUEL_CONCURRENCY 可覆盖）
    def submit(self, job_id, priority=0) -> bool          # 已在队列/运行 → False；按 (-priority, enq_seq) 排序
    def cancel_queued(self, job_id) -> bool               # 出队 → True
    def set_priority(self, job_id, priority) -> bool      # 仅 queued；重排序
    def release(self, job_id) -> list[str]                # 释放配额 → 可派发候选（幂等）
    def next_batch(self) -> list[str]                     # 配额内弹候选
    def queue_view(self) -> list[dict]                    # [{job_id, priority, position, enq_at}]
    def active_count(self) / running(self) -> ...
    def clear(self)                                       # 测试隔离
```

- mock job（url 以 `mock` 开头）**不进调度器**（沿用现有语义）。
- 位置 = 排序后下标；同优先级 FIFO（enq_seq）。

### 2.2 `storage.py`（扩展）

- `_job_state`：config.json 存在且无 tasks.json → `"queued"`（原 fallback `"pending"`
  仅无任何文件时保留；`test_storage` 两条断言演进）。
- `BATCHES_DIR = .eval/batches/`、`BATCH_ID_RE = ^batch_\d{8}_\d{6}_[0-9a-f]{6}$`、
  `save_batch/load_batch/list_batches`（结构见技术设计 §4.4）。
- 增量答案：`save_answers_inc(job_id, task_id, entry)`（`answers-inc.json` 幂等合并）、
  `load_answers_inc(job_id)`、`partial_answers_count(job_id)`。
- `get_job_files` 白名单不动（answers-inc 不入导出包，仅运行时内部）。

### 2.3 `executor.py`（扩展）

- `_execute_model(..., skip_ids: set[str] | None = None,
  persist_cb: Callable[[str, dict], None] | None = None)`：每题（含失败/重试后）
  调用 `persist_cb(task_id, entry)`；`skip_ids` 中的题跳过执行（不产生 entry，
  由调用方 merge 磁盘答案）。
- `execute_all` 透传两参数（双模型各一侧）。

### 2.4 `budget.py`（兼容扩展）

- `estimate_tokens(task_count, repeat_n, judge_mode="human", n_models=1)`：
  batch 用 `n_models=N` 放大；旧调用零破坏。

### 2.5 `schemas.py`（新增）

```python
class BenchmarkRequest(BaseModel):
    dataset_name: str
    model_ids: list[str] = Field(..., min_length=2, max_length=20)  # 模型配置库 id
    rounds: int = Field(1, ge=1, le=20)
    priority: int = Field(0, ge=-10, le=10)
    name: str | None = None
    review: ReviewConfig | None = None      # judge 配置（生成式题单臂）
    prompt_strategy: str = "cot"
    code_verify_mode: str = "off"
    budget: BudgetConfig | None = None
    embedding: EmbeddingConfig | None = None

class PriorityRequest(BaseModel):
    priority: int = Field(0, ge=-10, le=10)
```

### 2.6 `leaderboard.py`（单臂兼容扩展）

- `extract_job_model_scores`：verdict 无 `revealed` → 模型名取 `config.model_a.name`，
  `scores[].score`；有 revealed → 现状不变。零网络、确定性保持。

### 2.7 `main.py`（调度接线 + batch + resume + tasks API）

- **调度单例**：`_SCHEDULER = Scheduler(concurrency=int(os.environ.get(
  "MODEL_DUEL_CONCURRENCY", "2")))`；`async def _dispatch_pending()`：
  `next_batch()` → 逐 job 建 `asyncio.create_task(_run_eval(job_id))`
  （沿用 `_tasks`/`_task_done` 生命周期）。
- **start_eval 改造**：删除 429 门禁 → 校验（SSRF/数据集/预算）→ save_config →
  `_jobs` 建条目（state=`queued`、建 sse_queue、priority）→ `_SCHEDULER.submit` →
  `_dispatch_pending()` → 若未派发 `_push_event(queued)`；`_run_eval` 开头：
  state→executing + save_task_set + env 快照（排队时不落 tasks.json）。
- **配额释放**：`_scheduler_release(job_id)` 幂等，调用点：`_run_eval_impl` 进入
  reviewing 前（pure_human 与 hybrid 两处）、`_task_done`（completed/error/cancelled
  兜底）；release 后 `_dispatch_pending()`。
- **排队取消**：`delete_history` 分支——`_jobs[].state=="queued"` → `cancel_queued`
  + 删目录 + `audit.eval_cancelled`（无 task.cancel）；运行中保持现有 cancelling 语义。
- **`POST /api/eval/{job_id}/resume`**：磁盘态（`_jobs` 无条目）且 state∈
  {executing,error} 且 `partial_answers_count>0` → 重建 `_jobs` 条目（state=queued）
  → submit → dispatch；运行中 409、无部分答案 409、不存在 404；`_run_eval` 恢复
  路径开头 `load_answers_inc` → `skip_ids` + merge。
- **batch 路由**：`POST /api/benchmark`（校验：数据集、model_ids 全部在库且
  `has_key`、judge SSRF、预算 `n_models=N` hard 拒绝；批内 job 用模型库配置 +
  共享评测参数，`review.mode="single_arm"`）→ batch_id + N 个 job 逐个 submit
  （同优先级）→ 返回 `{batch_id, jobs}`；`GET /api/benchmark` 列表、
  `GET /api/benchmark/{batch_id}`（jobs 状态聚合 + progress + failed_models +
  leaderboard_id，轮询）、`GET /api/benchmark/{batch_id}/leaderboard`（落盘排行榜
  详情，复用迭代 6 结构 + lb_id）。
- **`_run_batch_job(job_id, batch_id)`**（新后台协程）：queued→executing（落
  tasks.json/env）→ 逐轮 `_execute_model`（M 轮，`answers-<label>-r{n}`）→ 评分
  （判别式 `score_task_metric` 逐轮均值 / 生成式 `run_single_arm_judge` 逐轮，
  judge 缺省 N/A + warning）→ 单臂 verdict + slim report → completed；error 分支
  置 error；终态回调 `_on_batch_job_done`（幂等计数 → 全部终态时聚合排行榜 +
  batch 置 done/partial + `audit.benchmark_done`）。
- **`GET /api/tasks`**：`{quota:{concurrency,active}, queued:[...], running:[...],
  batches:[...]}`。
- **`PUT /api/tasks/{job_id}/priority`**：queued 可改（重排序 + 审计
  `priority_changed`）；运行中/终态 409；不存在 404。
- **lifespan**：启动时沉降磁盘态 queued job → error.json。
- **audit**：`eval_queued`、`priority_changed`、`benchmark_started`、
  `benchmark_done`（含 partial 标志）、`eval_resumed`。

### 2.8 前端

- **`tasks.html`（新页，CSP nonce 托管）**：排队列表（位置/优先级/模型/取消/
  优先级调整，仅 queued 可调）、运行中列表（状态/进度/取消）、benchmark 面板
  （评测集 + 模型库多选 + 轮次 + judge 配置 + 优先级 → 创建批次）、批次列表
  （进度/失败 N/A/排行榜跳转）、轮询兜底（3s）。
- **`index.html`**：入口链接（任务列表）；`connectEvents` 增加 `queued` 分支文案
  「排队中（位置 N），等待调度…」。
- **leaderboard.html**：批次排行榜跳转直接复用（`?id=` 已支持）。

## 三、API 契约变更

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/eval/start` | 超并发不再 429，改为排队（200 + job_id；SSE `queued` 事件；位置/优先级随任务视图可见） |
| POST | `/api/eval/{id}/resume` | 磁盘态部分完成任务重新入队续跑（跳过已完成题；409/404 语义） |
| POST | `/api/benchmark` | 创建批次（model_ids 校验 Key 已补录；预算 hard 400） |
| GET | `/api/benchmark` / `{batch_id}` / `{batch_id}/leaderboard` | 列表 / 聚合进度（failed_models N/A）/ 排行榜详情 |
| GET | `/api/tasks` | 任务队列视图（排队位置/优先级/运行中/批次） |
| PUT | `/api/tasks/{job_id}/priority` | 改优先级（仅 queued，否则 409） |
| DELETE | `/api/history/{job_id}` | 排队中取消走独立分支（出队 + 删目录，无 task.cancel） |
| SSE | `/events` | 新增 `queued` 事件（排队中即可连接） |
| GET | `/api/eval/{id}/status` | state 新增 `queued`（运行中/磁盘态统一） |

## 四、测试计划（先回归 → 实现 → 全绿）

### 4.1 前置（第 0 步）

1. 提交迭代六工作区全部改动（如未提交）；2. 全量回归基线 902/30 全绿后固化。

### 4.2 新增测试文件

| 文件 | 覆盖 |
|---|---|
| `test_scheduler.py` | submit 排序（优先级/FIFO）、配额内派发、超配额排队、release 释放补派发、cancel_queued、set_priority 重排、mock 不计入、视图位置 |
| `test_batch.py` | 创建校验（Key 缺失 400/数据集 404/SSRF/预算 hard）；N=5 全链路（mock 单臂）→ done + leaderboard 聚合（分维度/胜率矩阵/CI）；部分失败 → partial + 失败模型 N/A；轮询状态机；审计事件 |
| `test_resume.py` | persist_cb 增量幂等落盘、skip_ids 跳过已完成题、merge 答案、resume API（运行中 409/无部分 409/不存在 404/磁盘态入队续跑跳过题数正确） |
| `test_tasks_api.py` | GET /api/tasks 视图、PUT priority（queued 可改/运行中 409）、排队取消分支（DELETE queued 语义 + 审计）、lifespan 排队沉降 error |

### 4.3 修改既有

- `test_limits.py`：并发 429 两用例 → 排队断言（200 + state queued + 队列位置）；
- `test_storage.py`：`_job_state` pending→queued 分支；
- `conftest.py`：`BATCHES_DIR` 重定向；
- `test_xss_playwright.py`：tasks.html 冒烟 + index 入口/queued 提示；
- README 并发描述更新（排队替代 429）。

### 4.4 验收映射

1. 多任务排队/抢占（优先级插队）/取消演示 → test_scheduler + test_tasks_api + tasks.html
2. 调度与续跑单测全绿 → test_scheduler/test_resume/test_tasks_api
3. N=5 benchmark 批次跑通 → test_batch（5 模型全链路 + 排行榜聚合）

## 五、实施顺序

1. 第 0 步：落盘本方案 + 全量回归基线
2. `scheduler.py` 纯逻辑 + 单测（test_scheduler）
3. storage：queued 状态/BATCHES_DIR/answers-inc/批次 CRUD + 既有单测演进
4. budget `n_models` + executor `skip_ids/persist_cb` + 单测（test_resume 前半）
5. main.py 调度接线（排队化/dispatch/release/排队取消/lifespan 沉降/tasks API）
   + test_tasks_api + test_limits 演进
6. batch：schemas/_run_batch_job/POST+GET benchmark + leaderboard 单臂扩展 + test_batch
7. resume API + test_resume 后半
8. 前端 tasks.html / index 入口 / playwright 冒烟
9. 全量回归 + 验收三要素汇报

## 六、风险与缓解

| 风险 | 缓解 |
|---|---|
| 429→排队行为变更破坏既有测试/文档 | 测试演进（test_limits 排队断言）+ README 更新；排队语义与设计文档 §6.3 一致 |
| queued 状态机与磁盘推断冲突 | `_job_state` 统一补 queued；tasks.json 延后到 dispatch 落盘；test_recovery 保持绿 |
| batch 单臂评审成本 N×M×题 | 预算预估 ×N hard 拒绝；judge 可缺省（生成式 N/A + warning）；判别式纯指标免评审 |
| 断点续跑重复/意外计费 | 仅磁盘态 + 显式 resume API；skip 集合判定 + 增量落盘幂等 |
| 重启丢队列 | v1 内存队列文档化 + lifespan 沉降 error 提示（不自动恢复） |
| 运行中任务被误解为可抢占 | 文档化：优先级仅排队中生效；运行中取消走现有 cancelling |
| batch 与常规 job 混跑配额耗尽 | 同池配额 + 优先级可让 batch 插队；批次进度轮询可见 |
