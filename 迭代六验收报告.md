# 迭代六验收报告：对抗鲁棒性、偏见测试与高级可视化

> 基线 commit `5b060c8`（迭代三收尾，708/30）→ 迭代四（781/30）→ 迭代五（816/30）→ **迭代六 902 passed / 30 skipped（0 失败）**
> 测试命令：`cd webapp && python -m pytest tests -q --tb=short -p no:cacheprovider`
> 新增用例：39（perturb/robustness/bias 纯函数）+ 16（dashboard）+ 11（perturb API）+ 18（leaderboard）+ 2（xss_playwright 页面冒烟）≈ 86 全绿

## 一、验收结论

| 验收项 | 结论 | 证据 |
| --- | --- | --- |
| 全量回归零失败 | ✅ | 902 passed / 30 skipped（迭代五基线 816 + 新增 86） |
| 对抗样本生成（改写/扰动/噪声注入，复用迭代 4 生成与校验能力） | ✅ | `engine/perturb.py` 三模式扰动器 + 复用 `generator.check_safety_static` 内容安全过滤（拦截样例有单测） |
| 鲁棒性衰减曲线（扰动强度 → 得分） | ✅ | `build_robustness_curves` 纯函数（强度 0=原版基线注入各模式曲线），`test_robustness.py` |
| 偏见与公平性测试（性别/地域/文化扰动对照） | ✅ | 属性扰动槽位表 + `bias_analysis`（分差/一致性/歧视阈值 1.0），`test_bias.py` |
| 图表类型齐全且有真实评测数据可演示 | ✅ | 排行榜页（排名表/胜率矩阵/雷达/箱线/散点/K-召回率）+ 扰动页（衰减曲线/偏见对照）+ KPI 看板（耗时/token 趋势/CPU），playwright 页面冒烟通过 |
| 3+ 模型排行榜演示数据可用 | ✅ | `POST /api/leaderboard` 由 N 个已完成 job 聚合（3 模型 last-wins 合并用例），bootstrap CI + 「差异不显著」标注 |
| KPI 看板（耗时/token 趋势/CPU，GPU N/A） | ✅ | `hwmon.py` 零依赖采样 + `kpi.duration_sec` + `GET /api/dashboard`，`test_dashboard.py` |
| 零真实网络 / 零密钥落盘 | ✅ | conftest 封锁 + 扰动/排行榜/看板全部 mock 上游；`_PERTURB_REQS` 仅内存含 Key，落盘仅掩码字段 |

## 二、功能验收明细

### 2.1 对抗扰动管线（engine/perturb.py，新增）

- 三种扰动模式：**改写**（同义词表非重叠匹配按强度比例替换）、**噪声注入**（按强度对字符流插入/删除/相邻交换）、**属性扰动**（性别/地域/文化槽位表一次性替换，防级联；单字性别代词负向断言防破坏「他们/其他」）。
- `perturb_task`：扰动版 id 加后缀 `-pN`、`meta` 记录 origin/mode/intensity，rubric/expected/context 保留——评审与指标口径与原版一致；代码题与不计分题（安全维）不参与扰动（记录 skipped 原因）。
- 扰动题同过 `check_safety_static` 内容安全过滤，intercepted 丢弃并记 warning。
- 得分口径统一 0-10：判别式 = top1/relaxed 命中率折算、代码题 = code_verify 通过率×10、生成式 = 单臂 rubric 评审分（`run_single_arm_judge` 复用，judge 缺省该类题 N/A + 提示）。
- 衰减曲线：按模式聚合 {强度 → 该强度全部题平均分}，强度 0.0 由原版基线注入每条曲线。
- 偏见对照：属性扰动题 vs 原版逐题 {分差、答案一致性（embedding 注入/离线 n-gram 兜底）、|分差|≥1.0 → 疑似歧视}。

### 2.2 扰动评测 API（独立异步任务，不动 job 状态机）

- `POST /api/perturb`：SSRF 校验 model/judge URL、非法模式 400、评测集 404、并发上限 3（429）；Key 仅存进程内存 `_PERTURB_REQS`（不落盘，重启遗留批次沉降 partial，仿 `_settle_generation`）。
- 后台协程：build_perturb_set → `executor._execute_model` 单模型作答 → 判别式指标/生成式单臂评审 → 组装 per_task + curves + bias + warnings 落盘 `.eval/perturb/<id>.json`。
- `GET /api/perturb` / `GET /api/perturb/{id}`（轮询进度；重启沉降 partial）。
- 审计：`perturb_started` / `perturb_completed`。

### 2.3 硬件利用率与 KPI 看板

- `backend/hwmon.py`（零新依赖）：`CpuSampler` 增量系统 CPU 利用率（Windows ctypes.GetSystemTimes / Linux /proc/stat / 其他 N/A），核心换算 `_util_from_deltas` 纯函数；`collect_gpu_util` 预留接口恒 None（N/A）。
- `_finalize_job` 落盘 `report["kpi"]["duration_sec"]`（内存 started_at 优先，磁盘路径回退 config.created_at，历史记录 N/A 空态）。
- `GET /api/dashboard`：`collect_hw()` + `build_jobs_trend`（completed job 的耗时/token 序列，`engine/dashboard.py` 纯函数 + 可注入 loader）。

### 2.4 N 模型排行榜（engine/leaderboard.py，新增）

- **数据来源（用户已拍板）**：N 个已完成 job（同一评测集）聚合——`extract_job_model_scores` 经 `verdict.revealed` 把 X/Y 侧得分归一化到真实模型名（含 answer_x_file 交叉映射），同模型多 job 同题 last-wins。
- 校验语义：job 不存在/未完成/无评审数据 400（`LeaderboardError`）、**评测集题集不一致 400（列出差异）**。
- 产出：综合分排名表（计分题合计，满分 10×题数，安全维不计入）、分维度排名表、胜率矩阵 + 配对 bootstrap CI（题数 <8 或 CI 含 0 → 「差异不显著」标注，`note` 口径说明）、K-召回率（Top-K 达标覆盖，阈值 6.0）、箱线/散点/雷达数据。
- `POST /api/leaderboard` / `GET` 列表 / `GET {id}`；落盘 `data/leaderboards/<lb_id>.json`；审计 `leaderboard_created`。
- 迭代 7 batch + 单臂评审接线后可替换数据源，聚合逻辑不变。

### 2.5 前端（3 个新页面 + 入口，CSP nonce 托管沿用）

- **perturb.html**：配置（模型库选择/手填、评测集、模式多选、judge 可选）→ 运行（2s 轮询）→ 衰减曲线（多折线）、偏见对照表（一致性/歧视徽标）、逐题明细、warnings。
- **leaderboard.html**：历史 job 多选 → 生成 → 综合分排名（🥇🥈🥉）、分维度排名（Top 高亮）、胜率矩阵 + CI 标注、雷达多模型叠加、箱线图、散点图、K-召回率多折线。
- **dashboard.html**：KPI 卡片（评测数/累计耗时/累计 Token）+ 耗时/token 趋势柱状 + CPU 增量采样显示（5s 轮询，首次采样 N/A 提示）+ GPU N/A + 明细表。
- index.html 高级评测入口三链接；`test_csp.py` PAGES 扩展至 8 页面全绿；playwright 页面冒烟（含扰动页全链路）2 用例。

## 三、实现中的问题修复记录

| 问题 | 修复 |
| --- | --- |
| 衰减曲线强度 0 缺失（原版分未注入各模式曲线） | `build_robustness_curves` 由各 origin 的原版分注入强度 0 基线 |
| 扰动背景任务在 asyncio.run 收尾被取消/完成（测试态） | 内存请求表 `_PERTURB_REQS`（Key 不落盘）+ 测试 `_drain` 幂等直跑管线 |
| 扰动测试 fixture 漏 patch 单臂评审 → 真实评审跑网络被封锁、invalid 题得分 0.0 | fixture 补 `run_single_arm_judge` patch；生成式得分经单臂 scores 投影（invalid 置 N/A + 警告） |
| playwright 页面 `echarts is not defined` | 新页面 echarts 引用改为 `/static/echarts.min.js`（挂载点） |
| `#dsSel option` 等待超时（option 可见性语义） | 改用 `wait_for_function` 断言 options.length |
| select_option 按 label 全匹配失败 | 改为按 `value` 选择 |
| 排行榜评测集一致性校验 | `build_leaderboard` 各 job tasks id 集合比对，不一致 400 列出差异 |
| `_PERTURB_REQS` 在取消路径被清（无法续测） | `else` 子句仅在正常完成时清内存请求；取消保留（进程将退出） |

## 四、遗留与说明

- 排行榜口径：v1 基于双盲成对评审分跨 job 聚合（`note` 字段明示），迭代 7 batch + 单臂评审接线后可换源。
- 属性扰动 v1 为内置规则槽位表（性别/地域/文化），无槽位命中的题跳过并记 warning；槽位表可后续扩展。
- CPU 利用率为系统级增量采样（首采返回空基线），GPU 预留接口恒 N/A；多进程部署下为宿主机口径。
- 扰动评测为独立异步任务（并发上限 3），不占用评测 job 并发配额；真实调用需部署方 API Key，测试全部 mock。
- 历史 job 无 `kpi.duration_sec` → dashboard 空态 N/A，新评测起自动有值。

## 五、建议后续（迭代七候选）

- 调度器/batch：benchmark 批次 = 1 任务集 × N 模型 × M 轮，排行榜聚合直接复用 `build_leaderboard`。
- 单臂评审接入主流程（迭代 3 协议层已就绪），排行榜换用单臂分提升跨模型可比性。
- 扰动管线接入 LLM 改写（当前纯规则同义词替换，语义扰动上限受限）。
- 评测包导出扩展：扰动/排行榜结果纳入可复现包。
