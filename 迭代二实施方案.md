# 模型评测平台 · 迭代二实施方案（完整定稿）

> 对应《迭代计划》迭代 2（指标引擎与统计体系 + AI 评审接入）。本文记录范围决策、D1~D3 实质缺陷修正、S1~S10 建议修正、API 契约与落地状态。

## 一、范围与决策确认

### 1.1 范围总表

| 事项 | 落地 | 说明 |
|---|---|---|
| `metrics.py` 指标注册表 | ✅ | 判别式 4 项 + 生成式 4 项，纯函数 |
| `embed.py` embedding provider | ✅ | auto 解析：external → local_bge 懒加载 → 离线字符 n-gram 兜底 |
| `extract.py` 答案提取分层 | ✅ | 规则链为主，LLM 兜底接口预留但默认关闭（无配置绝不外连） |
| 提示模板体系 | ✅ | `cot` 默认 / `direct` / `fewshot`（互斥回退） |
| 统计显著性 | ✅ | 配对 bootstrap CI + 胜率 + 两类「差异不显著」提示 |
| 截断/无效作答 | ✅ | 评审 prompt 标注 + 报告 warnings |
| 预算熔断 | ✅ | 预估公式 + hard（start 校验）/ warn（运行期幂等提醒） |
| KPI 结构化采集 | ✅ | 报告新增 `kpi` 段 |
| 报告指标分区 | ✅ | `metrics` 段（判别式/生成式）+ 前端分区渲染 |
| AI 评审接入 | ✅ | `review.mode: pure_human（默认）/ pure_agent`；hybrid 归迭代 3 |
| 生成式题参考答案 | ✅（D1 修正） | 8 个生成式题补 `expected` |
| embedding 默认 auto | ✅（D2 修正） | `provider=None` = auto（env→local→offline） |
| 迭代一基线固化 | ✅（D3 修正） | 实施第 0 步：提交迭代一工作区 + 全量回归 |
| 统计口径排除不计分题 | ✅（D3 修正） | significance/win_rate 按 `scoring_ids` 过滤 |
| 代码题跳过文本指标 | ✅（D3 修正） | 以 `code_verify` 为准，文本指标 N/A |

### 1.2 用户已拍板决策

1. 评审模式本次仅 `pure_human + pure_agent`，hybrid（混合加权/逐题随机交换/金标校准）留迭代 3
2. embedding 离线兜底 = 字符 n-gram 余弦（offline 档）
3. 默认提示策略 = **Zero-shot CoT**（历史可比性影响通过 `config.json` 落盘 `prompt_strategy` 字段自描述）

### 1.3 关键设计定案

- **embedding 采集前置到执行阶段**：向量在 executor 异步采集存答卷条目，报告生成保持「纯规则化统计、无 API」不变量；报告端语义相似度恒为纯函数（有向量→余弦，无向量→n-gram）
- **agent 评审复用 `human_review.build_final_verdict` 聚合**：按 job 既有 `reveal.json` 逐轮评审 → 轮级 verdict → 稳定空间跨轮聚合
- **`run_judge` 向后兼容改造**：新增 `revealed` 可选参数；缺省保持原随机行为，既有测试零破坏

## 二、模块设计

### 2.1 题库补充（D1）— `tasks.py` + `mock.py`

8 个生成式题补 `expected` 参考答案（T2E/T4C/T4D/T5C~T5G；安全题参考拒绝话术）；`mock.py` 对应维度补贴近 expected 的模板；`dataset_version` 升 v2。

### 2.2 `backend/engine/metrics.py`（新增）

- `METRICS` 注册表 + `compute_task_metrics(task, answer_entry, expected)`
- 判别式：`top1`/`exact_match`/`f1`（字符级）/`relaxed_accuracy`（isclose rel_tol=0.01/abs_tol=0.5）
- 生成式：`semantic_sim`/`rubric_score`/`consistency`（按 repeat_runs 有无，不硬绑 type）/`bleu`/`rouge_l`
- 代码维度题文本指标一律 N/A（以 code_verify 为权威）；截断/error 条目跳算记 warning
- 多子题（test_cases≥2）逐子题提取比对后平均

### 2.3 `backend/engine/embed.py`（新增）

- `resolve_provider(config)`：auto = env `MODEL_DUEL_EMBEDDING_*` → external；local_bge 可导入 → 用之；否则 offline
- `external` 走 `build_upstream_client`（SSRF/审计）；失败返回 None 向量降级 n-gram
- `local_bge` 懒加载 onnxruntime，未安装明确报错
- `offline` = 字符 bigram 计数余弦（确定性可测）

### 2.4 `backend/engine/extract.py`（新增）

规则链（纯 Python）：JSON 结构 → 选项字母/内容 → 结论句数字 → 代码块 → 兜底。`llm_call` 仅显式注入（默认 None），无配置返回原文，绝不静默外连。

### 2.5 `backend/engine/stats.py`（新增）

- `paired_bootstrap_ci`（配对 Δ=x−y 采样，`random.Random(seed)` 确定性）
- `win_rate` / `significance_note`（区分 CI 含 0 与样本 < MIN_SAMPLE=8 两类原因）
- 调用方须按 `scoring_ids` 过滤后传入

### 2.6 `executor.py`（修改）

- `build_prompt(task, strategy)`：cot 追加引导句（默认）；direct 原文逐字；fewshot 注入 test_cases 示例、无则回退 direct
- `execute_task` 增加 strategy；embedding 采集写 `ans_entry["semantic"]`（失败降级不中断）

### 2.7 `judge.py`（修改）

- `run_judge(..., revealed=None)`：显式映射不再随机；输出保留旧键 + 新增 `answer_x_file/answer_y_file`
- 换用 `build_upstream_client()`（SSRF 基线）
- 评审围栏：题目/答案文本截断（答案 ≤8000 字符）附「已截断」标注；truncated/error 答案显式状态标注
- 逐题 `progress_cb`（judging 进度）

### 2.8 `schemas.py`（修改，全可选向后兼容）

```python
class ReviewConfig(BaseModel):
    mode: str = "pure_human"            # pure_human | pure_agent
    judge: ModelConfig | None = None
    fail_open: bool = False

class BudgetConfig(BaseModel):
    max_tokens: int = 0                 # 0=不限制
    mode: str = "warn"                  # warn | hard

class EmbeddingConfig(BaseModel):
    provider: str | None = None         # None=auto
    url/key/name: ... | None

StartRequest += review / prompt_strategy="cot" / budget / embedding（全可选）
```

### 2.9 `main.py`（修改）— AI 评审接入

- 状态机：`executing → judging（agent）→ completed`；fail_open 降级 `reviewing`（reveal.json 已落盘可直接人工接手）；`pure_human` 维持现状
- `start_eval`：judge URL SSRF 校验；pure_agent 缺 judge → 400；预算 hard 超限 → 400；Key 不落盘（save_config 掩码泛化）
- `_run_eval_impl`：judging 状态 + SSE 进度 → 逐轮 run_judge → build_final_verdict(source="AI评审") → _finalize_job → completed；轮间取消检查
- warn 预算：每轮作答后累计校验，超限发一次 `budget_warning`（幂等）；hard 仅 start 校验
- judging 崩溃中间态：有 answers 无 verdict → 可删可查，不提供续审（迭代 7 覆盖）

### 2.10 `report_builder.py`（修改）

- `judge_mode` 按 `config.review.mode` 输出（human/agent），替代硬编码
- 新增 `metrics`（判别式/生成式分区）/`kpi`/`significance`/`warnings` 四段（既有字段不动）

### 2.11 `storage.py` / `audit.py` / `mock.py`

- `save_config` 掩码槽位泛化（+judge/embedding）
- audit 新增 `eval_judged`
- `mock.py` 补默认 review 字段；（可选）mock 支持 pure_agent 直接生成 verdict+report

## 三、API 契约变更

| 方法 | 路径 | 变更 |
|---|---|---|
| POST | `/api/eval/start` | StartRequest 新增 review/prompt_strategy/budget/embedding（旧请求体零破坏） |
| SSE | `/events` | 事件：judging（含 progress）、budget_warning、judge_health |
| GET | `/api/eval/{id}/report` | 新增 report.metrics/kpi/significance/warnings；judge_mode 按实际输出 |

## 四、前端变更

- index.html：评审配置面板（模式/评审 URL/Key/Name/fail-open）、提示策略 select、预算输入；SSE 分支新增 judging 进度与 budget_warning 提醒条
- report.html：判别式/生成式指标区、KPI 区、显著性 badge（两类文案）、warnings 列表、judge_mode 来源标注

## 五、测试计划（先回归 → 实现 → 全绿）

### 5.1 前置（第 0 步）

1. 提交迭代一工作区全部改动
2. 全量回归基线：470 passed, 30 skipped 全绿后固化

### 5.2 新增测试文件（9 个）

test_metrics / test_embed / test_extract / test_stats / test_prompts / test_budget / test_agent_judge / test_report_metrics / test_generative_expected

### 5.3 修改既有测试

test_judge（revealed/围栏）、test_report_builder（judge_mode/新段）、test_tasks（生成式 expected）

### 5.4 验收映射

1. 全部指标纯函数可测，mock 数据全绿 → test_metrics/stats/extract + 生成式指标真实数据非空
2. embedding provider 可切换，缺省离线可用 → test_embed
3. AI 评审接入后全链路（作答→评审→报告）mock 跑通 → test_agent_judge（零真实网络）

## 六、实施顺序

1. 第 0 步：提交迭代一 + 全量回归基线（D3）
2. schemas/storage 契约 + 掩码泛化
3. metrics / extract / stats / embed 纯函数层 + 单测
4. 题库 8 题 expected 补全 + mock 模板（D1）
5. executor 提示模板 + embedding 采集 + 单测
6. judge.py 改造 + 单测更新
7. report_builder 扩展 + 单测
8. 预算熔断 + 单测
9. main.py agent 评审接入 + test_agent_judge 集成
10. 前端 index/report
11. 全量回归 + 验收三要素汇报

## 七、风险与缓解

| 风险 | 缓解 |
|---|---|
| judge 改造回归 | `revealed` 缺省随机、输出保留旧键；test_judge 前置回归 |
| 报告结构破坏前端 | 仅新增字段，report.html 兼容渲染 |
| embedding external 不可用 | offline n-gram 兜底，语义指标不中断 |
| agent 评审拉长执行 | 逐轮取消检查 + 单题重试 1 次 + fail_open 降级 + judging 进度可视化 |
| 评审 Key 泄漏面 | save_config 掩码泛化 + redact_sensitive 纵深 + 审计脱敏 |
| 生成式题补 expected 质量风险 | 8 题人工编写、与 rubric_note 对齐、入库校验；评审仍以 rubric 为准，指标仅参考不参与计分 |
