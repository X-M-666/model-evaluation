# 模型对决评测平台的设计与实现

——一个支持双盲评审与代码隔离验真的大模型能力对比系统

**摘要**：随着大语言模型（LLM）的快速迭代，如何在统一、公平、可复现的条件下对比两个模型的真实能力，成为模型选型与应用落地的核心问题。本文设计并实现了一个"模型对决评测平台"，以"出题 → 双模型并发作答 → 人工双盲评审 → 报告生成"为主线，提供内置七大能力维度题库、自定义评测集、重复评测与多轮聚合、代码题隔离验真、效率与成本指标采集等完整能力。系统后端采用 Python FastAPI 异步架构，前端为原生 HTML/JavaScript + ECharts，数据以本地 JSON 文件持久化。针对平台自身的安全风险（访问控制、SSRF、凭据泄露、不可信代码执行、路径穿越等），本文建立了覆盖认证授权、资源限制、审计日志与 Windows 原生沙箱（AppContainer + Job Object）的多层防御体系，并构建了"纯函数单元测试 — 集成测试 — 端到端测试"三层自动化回归体系，通过 GitHub Actions 在 Linux/macOS/Windows 三平台持续集成。整个开发过程采用问题驱动的迭代模式，依据代码走读评审提出的十余个 issue 逐一闭环修复，最终形成 426 项测试全部通过、具备生产部署价值的安全评测平台。

**关键词**：大语言模型；双盲评测；FastAPI；AppContainer 沙箱；SSRF 防护；自动化测试；问题驱动开发

## 1 引言

### 1.1 研究背景与意义

大语言模型（LLM）能力的客观度量是一个长期挑战。厂商自报指标口径不一，公开榜单的题目可能进入模型训练语料，导致"刷榜"现象；而实际业务选型又往往依赖主观体验，缺乏可量化的横向对比依据。一个实用的评测平台应当满足三项基本要求：

1. **公平性**：双方模型在相同题目、相同提示词与相同评审规则下作答，评审过程不暴露答案归属，避免先入为主的偏见（双盲）。
2. **可复现性**：题目可固定种子随机抽取、重复轮次取平均，结果可用统计量（均值、中位数、标准差）描述，而不是一次性的偶然结论。
3. **安全性**：模型输出是不可信外部输入，尤其代码题可能被注入恶意代码，评测平台自身不能成为攻击载体。

### 1.2 本文工作

本文实现了一个满足上述要求的"模型对决评测平台"，主要工作包括：

- **评测全流程编排**：内置题库（七大能力维度）与自定义评测集的统一任务集建模，双模型异步并发执行，失败重试，效率（延迟/Token）指标采集；
- **人工双盲评审体系**：X/Y 身份每轮随机打乱、评审全程不暴露模型身份、多轮聚合（均值/标准差/中位数）与逐轮 reveal 持久化，评审结束才揭示身份；
- **安全加固**：单机/共享双部署模式、Bearer 认证与 Host/Origin 校验、写请求限流、SSRF 双层防护（校验层 + 连接层 DNS 重绑定防护）、API Key 全链路脱敏、SSE 短时单次 ticket、审计日志、Windows 原生代码隔离沙箱；
- **自动化测试体系**：三层测试 + 三平台 CI 矩阵，以回归测试守护安全与正确性不变量；
- **问题驱动的迭代过程**：通过代码走读评审发现问题（issue #3~#19），以"复现 → 根因 → 修复 → 回归测试"闭环逐项落地。

## 2 需求分析与总体设计

### 2.1 功能需求

平台面向"对比任意两个 OpenAI 兼容 API 模型"的场景，功能需求归纳如下：

| 编号 | 需求 | 说明 |
|---|---|---|
| F1 | 模型配置 | 填写 A/B 两模型的 URL、API Key、模型名及 temperature/max_tokens/top_p 高级参数，支持连通性测试 |
| F2 | 题库与抽题 | 内置七大维度题库池随机抽题，固定种子可复现；支持自定义评测集（JSON/CSV/Markdown/TXT 四种格式上传或 JSON 粘贴） |
| F3 | 并发执行 | 双模型并发调用，逐题采集延迟与 Token 消耗，单题失败不中断整轮，失败重试 1 次（指数退避） |
| F4 | 重复评测 | repeat_n>1 时自动运行 N 轮，报告取平均分 + 标准差，衡量稳定性 |
| F5 | 双盲评审 | 评审页仅显示"答案X/答案Y"，打分后自动生成 verdict、报告并揭示身份 |
| F6 | 代码验真 | 代码题默认仅语法检查；显式开启后可在隔离沙箱中逐测试用例执行 |
| F7 | 报告展示 | ECharts 雷达图、逐题评分表、7 段精确分析文本、答卷原文，支持导出 |
| F8 | 历史管理 | 评测记录与评测集持久化，可查看、删除；服务重启后未完成的评测可恢复 |

### 2.2 非功能需求

- **安全**：API Key 不落盘、不外泄；默认不执行不可信代码；对外接口有认证/CSRF/限流/SSRF 防护；审计日志可追溯关键操作；
- **可靠性**：评测任务可取消（同步语义删除）、服务重启后可恢复磁盘态任务、单点故障不中断整轮；
- **可维护性**：分层模块、统一校验入口、全链路回归测试，CI 三平台矩阵防止平台分支漂移。

### 2.3 总体架构

系统为前后端分离的单体 Web 应用，目录结构如下：

```
webapp/
├── backend/
│   ├── main.py              # FastAPI 路由 + 评测调度 + 任务生命周期
│   ├── schemas.py           # Pydantic 请求/响应模型
│   ├── storage.py           # .eval/history 评测记录库 + data/datasets 评测集库
│   ├── access.py            # 认证 / Host / Origin / 限流中间件
│   ├── security.py          # 敏感信息递归脱敏
│   ├── ssrf.py              # 上游 URL 校验 + 连接层 DNS 重绑定防护
│   ├── audit.py             # JSONL 审计日志
│   ├── sse_ticket.py        # SSE 短时单次 ticket
│   └── engine/
│       ├── tasks.py         # 内置七维题库 + 任务集生成
│       ├── executor.py      # 双模型异步并发执行器
│       ├── human_review.py  # 双盲映射 / 评审视图 / verdict 聚合
│       ├── judge.py         # AI 评审（评测体系用，Web 端人工评审）
│       ├── report_builder.py# 纯规则化报告生成（8 类图表 + 7 段分析）
│       ├── sandbox.py       # 代码提取 / 语法检查 / 用例执行编排
│       ├── isolation/       # 隔离后端：off / Windows 原生沙箱
│       ├── parsers.py       # JSON/CSV/MD/TXT 评测集解析器注册表
│       └── datasets.py      # 评测集统一校验 / 归一化
├── frontend/                # index.html / report.html / review.html
└── tests/                   # 三层自动化测试
```

**技术栈**：后端 Python 3.12 + FastAPI + httpx（异步并发）；前端原生 HTML/JS + ECharts（本地文件，无 CDN 依赖）；存储为本地 JSON 文件；CI 为 GitHub Actions 三平台矩阵。

### 2.4 任务生命周期与数据流

评测任务的状态机为：

```
executing（出题+双模型作答）→ reviewing（等待人工打分）→ completed
                                    ↓ 异常
                                  error
（运行中删除）→ cancelling → cancelled
```

关键设计点：

1. **后台任务管理**：`start_eval` 创建 `asyncio.create_task` 并保存引用，挂 `add_done_callback` 回收异常；删除运行中任务采用**同步语义**——先置 `cancelling`、取消任务并等待其安全终止（最长 5 秒兜底）后才删除内存状态与磁盘目录，杜绝"删除后目录复活"竞态。
2. **取消协议**：`_run_eval` 在每次持久化与状态转换前检查取消标记，协作式取消保证任何 await 点被中断后不会写入任何文件。
3. **磁盘恢复**：任务集/答卷/verdict/report 全部落盘；服务重启后 `_load_job_state` 从磁盘重建评审所需状态，评审提交与报告生成不依赖进程内 `_jobs` 全局（issue #5）。

## 3 关键模块设计与实现

### 3.1 内置题库与任务集生成

题库覆盖七大能力维度（知识、推理、代码、语言、指令遵循与对齐、长文本与多模态、效率与稳定性），每维度 2 道备选题以避免重复评测时题目雷同。题目形态包括：

- **知识题**（MMLU/C-Eval 风格）：三选一综合单选，期望答案按"题号+字母+内容"严格核验；
- **推理题**（自拟）：三阶幻方枚举、排列组合等多步推理，评分区分结论分与过程分；
- **代码题**（LeetCode/HumanEval 风格）：区间合并、两数之和等，内置 5 组带期望输出的 test_cases（期望值经手工推演验证）；
- **指令遵循题**（IFEval 风格）：JSON 硬约束、格式与禁词约束，逐条违反即整条 0 分；
- **长文本题**（L-Eval/LongBench 风格）：1000+ 字虚构材料 + 细节提取与跨段整合；
- **效率与稳定性题**（自定义）：流式统计 + 稳定排序，要求单遍扫描 O(n)，自动重复运行 2 次（第二次 temperature=0.0）校验输出逐字节一致性。

任务集生成器 `build_task_set(dims, seed, num_questions)` 支持固定种子复现；自定义评测集经统一校验后转换为与内置题库完全相同的结构（`build_task_set_from_dataset`）。

### 3.2 双模型并发执行器

`executor.py` 实现双模型 `asyncio.gather` 并发执行，每个模型内部串行逐题调用 OpenAI 兼容 `POST {URL}/chat/completions`。设计要点：

- **指标采集**：每题记录 `latency_ms`、`prompt_tokens`、`completion_tokens`、`finish_reason`（truncated 标记），写入答卷 `api_info`；
- **失败容忍**：单题失败重试 1 次（指数退避，超时 120s），整轮不中断；`return_exceptions=True` 吞掉单模型整体异常并生成空答卷，保证另一模型结果仍可用；
- **代码验真接入**：代码题在 `_CODE_SEM=2` 信号量下调用隔离后端逐用例执行，沙箱异常（含部分执行）不中断整轮，错误信息随答卷返回；
- **稳定性题双跑**：效率与稳定性维度题目自动 `repeat_index=1/2` 两次运行（第二次 temperature=0.0），供报告做输出一致性判定。

### 3.3 人工双盲评审与多轮聚合

评审是"公平性"的核心，本模块与 AI 评审完全解耦，不调用任何评审模型，保证流程免费可离线：

- **双盲映射**：`make_reveal(rounds)` 为每轮独立随机决定答案X/答案Y 对应 A/B 答卷；评审视图 `build_review_view` 只暴露题目、评分标准与 X/Y 答案内容，模型身份字段全部隐藏；映射持久化到 `reveal.json`，重启不丢；
- **集合级校验**：评审提交前 `_validate_review_scores` 校验每个 (round, task_id) 恰好出现一次、题号归属任务集、轮次在 1..repeat_n，任何违规整体 400 拒绝、零副作用（issue #9），杜绝"缺失题自动 0 分仍生成正式报告"；
- **多轮聚合**：`build_final_verdict` 先在**稳定模型空间**（model_a/model_b）按每轮 reveal 归一化，再聚合均值/标准差/中位数与维度汇总，最后用固定投影映射回 X/Y 展示字段，保证跨轮 X/Y 身份交换不影响统计正确性（issue #10）。

### 3.4 报告生成

`report_builder.py` 为纯规则化统计（无 API 调用），输入任务集、答卷与 verdict，输出：

- **8 类图表数据**：逐题得分、维度得分、延迟、Token、代码通过率、效率散点、胜平负分布、稳定性（标准差/中位数）；
- **7 段精确分析文本**：总体结论、得分剖析、效率剖析、成本剖析（含单位得分成本）、代码质量、稳定性、建议，每段逐条引用具体数值；
- **多轮口径一致**：多轮时效率/成本/代码/原文按稳定模型跨轮聚合（延迟取平均、Token 求和、成功率=成功轮/总轮），杜绝"多轮评分 + 最后一轮指标"的口径混用（issue #10）。

### 3.5 评测集解析与统一校验

支持 JSON / CSV / Markdown / TXT 四种格式上传与 JSON 粘贴，各格式经解析器注册表（`parsers.py`）转换为统一标准结构后，全部进入 `datasets.py` 的统一校验层：

- `DatasetValidationError` 携带字段路径（如 `tasks[2].prompt`），错误信息可操作；
- 类型安全取值器替换裸 `.strip()`，数字/对象等非法类型返回结构化 400 而非 500（issue #15）；
- 任务 id 强制类型、非空、唯一；`MAX_DATASET_TASKS=200` 等长度上限统一收口；
- 上传文件名（stem）同样过名称校验，避免"上传成功但启动评测被拒"的体验不一致。

## 4 安全设计

平台遵循"纵深防御 + 默认不执行不可信代码 + fail closed"的安全原则，分六层：

### 4.1 访问控制（issue #8）

两种部署模式由中间件 `security_middleware` 统一实施：

- **单机模式**（默认）：仅监听 `127.0.0.1`，服务端校验 Host 为回环别名，阻止 DNS rebinding 与局域网直连；
- **共享模式**（设置 `MODEL_DUEL_TOKEN`）：全部 `/api/*` 与 `/docs` 等接口文档需 `Bearer` 令牌（`secrets.compare_digest` 恒定时间比较）；写请求校验 Origin/Referer 与 Host 同源（CSRF）；写请求按 IP 滑动窗口限流（默认 30 次/分钟，追踪表上限 10000 IP 防无限增长）；
- **资源限制**：评测并发上限 2、上传 5MB、JSON 粘贴 2MB、数据集 200 题；请求体流式截断读取，超限在读取阶段即拒绝，不整体读入内存。

### 4.2 SSRF 防护（issue #12、#19）

模型 URL 为外部输入，若不校验可被用作内网探测面甚至读取云元数据。防护分两层：

- **校验层** `validate_upstream_url`：仅允许 http/https 公网目标，解析全部 IP，任一 IP 非公网（回环/私网/链路本地/CGNAT/云元数据/组播）即整体拒绝；IP 字面量直接校验；
- **连接层** `ValidatingNetworkBackend`：经 httpcore 官方扩展点在**每次建立 TCP 连接前**重新解析并逐 IP 校验（不缓存），将已验证 IP 绑定给连接，彻底封堵"校验时返回公网 IP、连接时切回内网"的 DNS 重绑定绕过；TLS 仍以原始域名做 SNI/证书校验，多地址故障转移显式捕获 `httpcore.ConnectError/ConnectTimeout`（MRO 上并非 OSError 子类，修复后首个地址失败会继续尝试后续地址）；
- 内网自建模型场景显式设置 `MODEL_DUEL_ALLOW_PRIVATE_UPSTREAM=1` 放行，不做静默降级。

### 4.3 凭据保护与脱敏

- API Key 仅保存在后端内存，`save_config` 落盘前统一打码为 `***`；
- `security.py` 的 `redact_sensitive` 递归删除所有命名敏感字段（key/api_key/token/authorization/secret），历史/报告接口响应统一经过，作为读层纵深防御（含修复前遗留旧记录）；
- 审计日志仅记录白名单字段（ts/event/job_id/target/path/actor），写入前递归脱敏，API Key 永不进入日志。

### 4.4 SSE 短时单次 Ticket（issue #13）

原生 EventSource 无法携带自定义 header，若将长期管理 Token 拼入 URL 会泄露给反向代理/WAF/访问日志。方案：

- 新增签发接口 `POST /api/eval/{job_id}/events/ticket`（经认证 + Origin 校验 + 限流），签发 60 秒、单次、job 作用域的随机 ticket（容量上限 10000，惰性清理）；
- `/events` 路由仅接受 Bearer header 或单次 ticket，`?token=` 一律 401；
- 中间件无条件从 `scope["query_string"]` 剥离 ticket/token 参数（含 401 路径），保证 uvicorn 与代理日志无凭据残留；
- 携带失效 ticket 的自动重连静默 204 且不记审计（ticket 为 128 位随机单次凭证，无爆破价值），未携带 ticket 的未认证请求仍 401 + 审计。

### 4.5 代码隔离沙箱（issue #3/#4/#16/#18）

模型输出的代码属于不可信输入，安全边界必须位于**操作系统层**而非 Python 语言级黑名单：

- **默认不执行**：`code_verify_mode=off` 仅 `compile()` 语法检查 + 展示；显式选择 `native-sandbox` 时启动前强制校验沙箱可用（fail closed）；
- **AppContainer**（Windows）：空能力集容器进程，容器 SID 未授权路径一律拒绝（文件硬隔离），默认无网络能力；一次性工作目录经 `icacls` 显式授予容器 SID；
- **Job Object 配额**：内存上限 256MB、CPU 时间 5s、活动进程数 8、`KILL_ON_JOB_CLOSE` 超时强杀整棵进程树；
- **环境白名单**：不继承宿主环境变量，仅 TEMP/TMP/LOCALAPPDATA/SYSTEMROOT 等，LOCALAPPDATA 指向工作目录；
- **存储软配额**：监视线程每 0.05s 轮询整个工作目录总占用（默认 1MB），超限 `TerminateJobObject` 强杀；进程退出后复核兜底（覆盖单次高速大写入窗口竞态），记录 `peak_dir_bytes` 峰值，清理失败显式标记（issue #18）；
- **并发互异**：固定 4 个 AppContainer profile 池轮换分配，保证并发运行的 SID 互异，杜绝共享 ACL 导致的跨任务数据泄露；
- **运行时由部署方提供**：应用不下载、不解压、不安装 Python（消除供应链 RCE 面），`MODEL_DUEL_SANDBOX_PYTHON` 必须指向自包含单目录运行时，路径/文件类型/版本（≥3.10）/自包含性（拒绝 venv）全量校验（issue #16）；
- 非 Windows 平台自动回退 off，**不存在**"无隔离直接执行"的路径。

### 4.6 其他安全加固

- **路径穿越防护**（issue #17）：job_id 必须匹配 `^\d{8}_\d{6}_[0-9a-f]{6}$` 格式，且 resolve 后必须为 BASE_DIR 直接子目录，URL 编码 `%2E%2E` 解码后无法越界删除 `.eval` 等上级目录；
- **前端 XSS 防护**（issue #7）：废弃 `innerHTML` 字符串拼接与内联事件处理器，全部改用 DOM API（createElement/textContent/addEventListener）；CSP 仅放行本站内联脚本（nonce 每请求轮换），`object-src 'none'; base-uri 'none'; frame-ancestors 'none'`；
- **审计日志**：评测启动/评审提交/历史与数据集删除/鉴权失败等关键事件 JSONL 追加至 `.eval/audit.log`，审计异常静默不影响主流程。

## 5 自动化测试体系

测试体系守护三类核心不变量：**安全**、**结果正确性**、**可恢复性**。所有测试零真实网络（全局网络封锁 fixture + 内存 mock 模型），存储重定向到临时目录不污染仓库。

### 5.1 分层测试

| 层 | 测试文件 | 覆盖内容 |
|---|---|---|
| 纯函数单元 | test_parsers / test_datasets / test_storage / test_tasks / test_report_builder / test_human_review / test_security / test_ssrf | 四种评测集格式正常/边界/恶意输入、配置脱敏落盘、状态推断、损坏 JSON、七维题库完整性、X/Y 身份映射、Key 不落盘 |
| FastAPI 集成 | test_access_control / test_limits / test_recovery / test_review_validation / test_report_reveal / test_report_rounds / test_audit / test_proxy_logs / test_sse_ticket / test_task_lifecycle / test_path_traversal / test_csp / test_bootstrap / test_code_runner | 认证/Host/Origin、限流并发上传、磁盘态任务重启后可评审、评分完整性唯一性、reveal 映射与多轮聚合、审计白名单、真实 uvicorn 日志中 ticket/Token 不残留、取消与删除语义、job_id 越界拒绝 |
| 浏览器端到端 | test_xss_playwright（本地，可选） | 前端 XSS 实弹验证，未装 playwright 自动跳过 |
| 平台专属 | test_sandbox（`native` 标记，Windows） | 宿主文件读取拒绝、目录外写入拒绝、网络拒绝、内存/CPU/进程配额、磁盘占用软配额四例（普通文件持续写入、多文件累计、单次大写入复核、峰值上界 ≤ 配额+64KB）、环境不继承、并发 SID 互异 |

### 5.2 CI 三平台矩阵

GitHub Actions 在 Linux / macOS / Windows 三平台自动执行：公共测试（`-m "not native"`）三平台全跑；Windows 额外执行 `sandbox_selfcheck` 真实受限进程自检与全量原生沙箱测试，防止平台分支漂移与"全部 skip 使功能看似已验证"的假象。

## 6 开发过程：问题驱动的迭代

### 6.1 开发流程

项目采用"原型快速落地 → 代码走读评审 → 问题驱动迭代"的开发模式：每轮评审产出带严重度与可复现路径的 issue，修复遵循"先加入能失败的回归测试（红），再实施修复使其变绿"，并在修复文件中记录 issue 编号与评审轮次（R2/R3）以便追溯。

### 6.2 主要问题与修复闭环

| Issue | 严重度 | 问题概要 | 修复方案 |
|---|---|---|---|
| #3 | 严重 | 沙箱用 Python 语言级黑名单隔离，`io` 等可绕过读取宿主文件；无内存/CPU/进程配额 | 边界上移 OS 层：AppContainer + Job Object；默认不执行（off） |
| #4 | 中 | 沙箱前缀覆盖 `sys.stdin`，函数/程序两种测试输入全失效 | 删除 stdin 覆盖，按 input 形态区分函数型/程序型执行 |
| #5 | 高 | 服务重启后 `_finalize_job` 访问空 `_jobs` 抛 KeyError，评审无法完成 | `_load_job_state` 磁盘恢复 + `_finalize_job` 全显式参数化 |
| #6 | 中 | 报告页原文硬编码 answers_a→X，与 X/Y 随机映射矛盾 | `reveal_answers` 按 reveal 归一化，前端统一消费 |
| #7 | 高 | 前端 `esc()` 上下文混淆，数据集名单引号注入 XSS | 全部改 DOM API + CSP nonce |
| #8 | 严重 | 默认监听 0.0.0.0 且无认证/CSRF/限流 | 单机/共享双模式、Bearer 认证、Origin 校验、限流、资源上限、审计 |
| #9 | 高 | 评审提交缺集合级校验，缺失题自动 0 分仍出正式报告 | `_validate_review_scores` 完整性/唯一性/归属校验，违规 400 零副作用 |
| #10 | 高 | 多轮报告混用多轮评分与最后一轮指标 | 每轮落盘 `answers-*-r{n}`，报告按稳定模型跨轮聚合 |
| #11 | — | 无自动化测试体系 | 三层测试 + 三平台 CI 矩阵 |
| #12 | 严重 | 校验与连接两次独立 DNS 解析，可 DNS 重绑定绕过 | 连接层 `ValidatingNetworkBackend` 连接前即时校验 |
| #13 | 高 | 长期 Token 进 SSE URL，暴露给代理/日志 | 短时单次 ticket 机制 + 查询参数剥离 |
| #14 | 高 | 删除运行中任务不取消后台协程，目录复活 | 任务注册 + 协作取消协议 + 同步语义删除 |
| #15 | 高 | 评测集非法类型 500、重复 id 造成评审死锁 | 统一校验层 `validate_standard_dataset` |
| #16 | 严重 | 应用内建 URL 自动下载解压 Python，供应链 RCE 面 | 删除下载路径，运行时由部署方配置并全量校验 |
| #17 | 严重 | `%2E%2E` 编码 job_id 可越界删除 `.eval` 目录 | job_id 格式校验 + 直接子目录断言 |
| #18 | 高 | 磁盘配额只监视 stdout/stderr 两文件，可写任意文件绕过 | 监视整个工作目录 + 退出复核 + 峰值量化 + 清理可观测 |
| #19 | 中 | 多 IP 故障转移捕获 OSError，生产异常契约不一致 | 精确捕获 `httpcore.ConnectError/ConnectTimeout` |

此外，第三轮复审（R3-001）对 SSE ticket 方案提出 4 项残余风险（401 路径参数未剥离、EventSource 自动重连噪声、代理日志无自动化验证、ticket 内存残留），均已落地修复并新增 `test_proxy_logs.py` 真实 uvicorn 日志验证。

### 6.3 评测体系的自举

项目自带的 `.eval/` 评测体系（designer 出题 → executor 双模型作答 → judge 双盲评审）既是平台题库的验证工具（七维题目与测试用例期望值均经双盲评审核验），也是开发过程的"题目来源"——内置题库的题目全部复用 `.eval/tasks.json` 中经过双盲评审与测试用例推演验证的题面，形成"平台测模型、体系验题目"的闭环。

## 7 运行验证与结果

### 7.1 功能验证

- **完整流程**：配置模型 → 选维度抽题（固定种子）→ 双模型并发作答（SSE 实时进度）→ 盲评页打分 → 报告页（雷达图/逐题表/7 段分析/原文）→ 导出 JSON，全链路验证通过；
- **演示模式**：内置 mock 数据生成，无需 API Key 即可体验完整报告效果；
- **代码验真**：`python -m scripts.sandbox_selfcheck` 自检通过后，区间合并等代码题 5 组用例在沙箱内逐组执行；宿主文件读取、网络连接、内存炸弹、磁盘填满等恶意场景均被系统级边界拦截；
- **恢复性**：服务重启后磁盘态任务可继续评审并完成闭环。

### 7.2 测试结果

完整测试套件 **426 passed**（含 Windows 原生沙箱测试与真实 uvicorn 代理日志验证），覆盖三平台 CI；带覆盖率的全量测试在本地运行通过。

## 8 总结与展望

### 8.1 总结

本文设计并实现了模型对决评测平台，将"出题、双模型作答、人工双盲评审、报告展示"的完整评测流程产品化。项目在三个维度具有代表性：

1. **评测方法学**：双盲 X/Y 随机映射 + 多轮聚合统计 + 逐轮 reveal 审计，把"公平对比"从口号落实为可审计的工程实现；
2. **安全工程**：以"操作系统边界而非语言黑名单"的沙箱设计、连接层 DNS 重绑定防护、全链路凭据脱敏为代表，体现了对不可信输入与网络攻击面的系统性防御；
3. **工程质量**：问题驱动的迭代 + 回归测试先行 + 三平台 CI，使安全修复可验证、功能演化不退化。

### 8.2 展望

后续可扩展方向包括：多平台代码隔离后端（Linux 的 Landlock/namespaces、macOS 的 sandbox-exec）以消除平台差异；AI 评审与人工评审的混合模式（AI 初评 + 人工抽检）；基于 SQLite 的持久化以支撑更大规模历史查询；支持流式输出与更多模型协议（Anthropic/Google 兼容格式）；以及面向"模型间对抗出题"的自动化题库扩充。

## 参考文献

[1] 刘知远, 等. 大语言模型[M]. 北京: 电子工业出版社, 2023.

[2] Vaswani A, Shazeer N, Parmar N, et al. Attention is all you need[C]. NeurIPS, 2017.

[3] FastAPI. FastAPI documentation[EB/OL]. https://fastapi.tiangolo.com.

[4] httpx. HTTPX: Python HTTP client[EB/OL]. https://www.python-httpx.org.

[5] OpenAI. ChatGPT 评测与开源社区基准 [EB/OL]. https://openai.com.

[6] Microsoft. AppContainer for Windows desktop applications[EB/OL]. https://learn.microsoft.com/windows/win32/secauthz/appcontainer-for-legacy-applications.

[7] Microsoft. Job Objects[EB/OL]. https://learn.microsoft.com/windows/win32/procthread/job-objects.

[8] OWASP. Server-Side Request Forgery Prevention Cheat Sheet[EB/OL]. https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html.

[9] Zhou J, et al. Instruction-Following Evaluation for Large Language Models (IFEval)[C]. ICLR, 2024.

[10] Hendrycks D, et al. Measuring Massive Multitask Language Understanding (MMLU)[C]. ICLR, 2021.

[11] An G, et al. L-Eval: Instituting Standardized Evaluation for Long Context Language Models[C]. ACL, 2024.
