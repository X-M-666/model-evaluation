# 模型评测平台 · 迭代一实施方案（含 D1~D4 修订）

> 对应《需求迭代文档》迭代 1（任务模型与评测集地基）。本文记录已确认的范围取舍、D1~D4 修订要点、API 契约与落地状态。

## 一、范围确认（用户已拍板）

| 事项 | 决策 |
|---|---|
| 题量 | 数学 / 安全 / 语言生成式题均按基准量配置（不再追加） |
| MD/TXT 格式 | 全格式（JSON/CSV/MD/TXT）支持 `type` / `context` 解析 |
| 模型配置库 | 只做「下拉填入模型 A/B」；Key 无内存时手动补录（页面提示），不做自动校验连通性 |

## 二、D1~D4 修订要点

### D1：task schema 校验（datasets.py）

- 判别式任务：`expected` 或 `test_cases` 至少其一（**仅显式声明 `type=判别式` 时强制**）
  - `test_cases` 每项含 `input` 与 `expected`
- 生成式任务：`rubric_note` 必填；缺省由 `BUILTIN_RUBRICS` 兜底
- `type` 缺省 = 判别式；**未显式 type 的旧格式宽容放行**（构建阶段补缺省 rubric，零破坏）
- `context` 长度上限 `MAX_CONTEXT_LEN = 32000` 字符
- `tags` 每项 ≤ `MAX_TAG_LEN = 50` 字符、每题 ≤ `MAX_TAGS_PER_TASK = 10` 个
- CSV 无 expected 列时：全部行显式 `type=生成式` 才放行；生成式行缺 rubric 补缺省生成式 rubric
- 全格式（带 dimension）仅有 expected 时：派生 `test_cases`（老格式兼容）
- **实现坑（已固化到测试）**：`explicit_type` 必须在 `t["type"]` 缺省赋值前捕获，否则恒为 True

### D2：八维体系落地

- `DIMENSIONS` 重定义；`executor.py` 引用 `CODE_DIMENSION`/`STABILITY_DIMENSION`，删除魔法字符串
- `QUESTION_POOL` 重配：
  - 数学（T2E）、语言（T4C/T4D）、安全（T5C~T5G，`excluded_from_total=True` 生成式）、长文本（T6/T6B/T7/T7B）
- 全池统一补 `context`/`tags`/`excluded_from_total` 缺省；meta 增加 `dataset_version=v1` / `dataset_source=builtin` / `scope=八大能力维度`
- `build_task_set_from_dataset` 透传 version/source；mock 八维答案模板已同步

### D3：模型配置库（models_registry.py 新增）

- 落盘 `webapp/data/models/<id>.json`：id/name/url/温度/采样参数/`key_masked`（**Key 永不落盘**）
- 进程内存 Key 表 `{id: key}`（`set_key`/`get_key`/`clear_memory_keys`）
- **API 契约（已固化为测试）**：
  - `POST /api/models` 成功 → `{"ok": True, "model": info}`；重名 → 400（非 409）
  - `GET /api/models` → 列表，脱敏 `key_masked`/`has_key`
  - `GET /api/models/{id}` → `{**info, "key": get_key(model_id)}`（**单条附带进程内存 Key**，供前端填入流程）
  - `DELETE /api/models/{id}` → 删除并清内存 Key；缺失 → 404
- SSRF：注册即 `ssrf.validate_upstream_url`（测试环境 `ALLOW_PRIVATE` 未开启，合法 URL 须公网 IP；`https://8.8.8.8/v1` 合法 / `http://10.0.0.1/v1` 拒绝）
- 审计：`model_registered` / `model_deleted` 事件

### D4：跨 job 汇总与不计分题

- `storage.py`：`STATS_DIR`/`SATURATION_FILE`、`update_saturation`（job_id 幂等去重）、`get_saturation`
- `judge.py`：聚合仅统计 `scoring_ids`（排除 `excluded_from_total` 题）；meta 记录 `excluded_ids`/`excluded_dimensions`；新增 `_task_flag`
- `human_review.py`：round/final verdict 排除不计分题（保留逐题展示）；final verdict 的 answer_x/answer_y 投影末轮 X/Y 映射
- `report_builder.py`：summary 用 scoring_ids（win/ties/avg/max_score/scoring_count）；新增「不计分维度」分析段
- 接口：`GET /api/stats/saturation?dataset=...`
- 前端：模型 A/B 下拉（`a-lib`/`b-lib`）、模型配置库卡片（列表/填 A/填 B/删除）、数据集列表与上传反馈展示 type_counts/version/source、任务类型徽标

## 三、测试与回归状态

- 新增：`test_models_registry.py`（15）、test_tasks 八维断言、test_datasets 迭代一（15）、test_parsers +5、test_storage +7、test_judge +2、test_human_review +2、test_report_builder +3、test_xss_playwright +4
- `conftest.py` `_isolate_storage` 额外重定向 `STATS_DIR`/`SATURATION_FILE` 与 `MODELS_DIR`，并 `clear_memory_keys()`；`test_xss_playwright.py` 模块级 fixture 同步
- 前端 e2e 采用按行定位（`has_text=` 定位到具体模型行再点按钮，避免多行 strict 冲突）
- **全量回归：470 passed, 30 skipped**（含既有原生 skip），无失败

## 四、遗留与下一步

- [x] 前端 `applyModelFromLibrary` 改为填入时 `GET /api/models/{id}` 取 Key（列表不再依赖 `m.key`）
- [x] `test_dataset_list_shows_type_badges` 选择器精确化为 `#dataset-list .ds-item`
- [ ] 迭代 2（指标引擎/统计体系/AI 评审接入）按《迭代计划》推进
